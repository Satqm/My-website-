<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#2c3e50">
    <meta name="description" content="CA Final Professional Offline Tracker - Advanced study planning and progress analytics">
    <title>CA Final Pro Tracker</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="manifest" href="manifest.json">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/crypto-js@4.1.1/crypto-js.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        :root {
            --primary: #2c3e50;
            --secondary: #3498db;
            --accent: #e74c3c;
            --success: #2ecc71;
            --warning: #f39c12;
            --light: #ecf0f1;
            --dark: #2c3e50;
            --gray: #95a5a6;
            --shadow: 0 4px 12px rgba(0, 0, 0, 0.1);
            --gradient: linear-gradient(135deg, #2c3e50 0%, #3498db 100%);
        }
        
        body {
            background-color: #f8fafc;
            color: #333;
            line-height: 1.6;
            overflow-x: hidden;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* License Screen */
        .license-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: var(--gradient);
            position: relative;
            overflow: hidden;
        }
        
        .license-container::before {
            content: '';
            position: absolute;
            width: 300%;
            height: 300%;
            background: 
                radial-gradient(circle at 20% 80%, rgba(255,255,255,0.1) 1px, transparent 1px),
                radial-gradient(circle at 80% 20%, rgba(255,255,255,0.1) 1px, transparent 1px);
            background-size: 100px 100px;
            animation: float 20s linear infinite;
        }
        
        @keyframes float {
            0% { transform: translate(0, 0) rotate(0deg); }
            100% { transform: translate(-100px, -100px) rotate(360deg); }
        }
        
        .license-box {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 24px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 500px;
            padding: 40px;
            position: relative;
            z-index: 1;
            border: 1px solid rgba(255, 255, 255, 0.3);
            animation: slideUp 0.5s ease;
        }
        
        .license-header {
            text-align: center;
            margin-bottom: 40px;
        }
        
        .license-logo {
            width: 100px;
            height: 100px;
            background: var(--gradient);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px;
            color: white;
            font-size: 40px;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }
        
        .license-header h1 {
            font-size: 32px;
            color: var(--primary);
            margin-bottom: 10px;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .license-header p {
            color: var(--gray);
            font-size: 16px;
        }
        
        .form-group {
            margin-bottom: 24px;
            position: relative;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 10px;
            font-weight: 600;
            color: #555;
            font-size: 15px;
        }
        
        .form-group input, .form-group select, .form-group textarea {
            width: 100%;
            padding: 16px 20px;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            font-size: 16px;
            transition: all 0.3s;
            background: white;
        }
        
        .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
            border-color: var(--secondary);
            outline: none;
            box-shadow: 0 0 0 4px rgba(52, 152, 219, 0.1);
        }
        
        .btn {
            display: inline-block;
            padding: 16px 32px;
            background: var(--gradient);
            color: white;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            transition: all 0.3s;
            width: 100%;
            position: relative;
            overflow: hidden;
        }
        
        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(52, 152, 219, 0.3);
        }
        
        .btn:active {
            transform: translateY(-1px);
        }
        
        .btn-accent {
            background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
        }
        
        .btn-accent:hover {
            box-shadow: 0 15px 30px rgba(231, 76, 60, 0.3);
        }
        
        .license-message {
            margin-top: 20px;
            padding: 15px;
            border-radius: 12px;
            text-align: center;
            font-weight: 500;
            animation: slideIn 0.3s ease;
        }
        
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .license-message.success {
            background: rgba(46, 204, 113, 0.1);
            color: #27ae60;
            border: 1px solid rgba(46, 204, 113, 0.2);
        }
        
        .license-message.error {
            background: rgba(231, 76, 60, 0.1);
            color: #c0392b;
            border: 1px solid rgba(231, 76, 60, 0.2);
        }
        
        .demo-video {
            margin: 30px 0;
            text-align: center;
        }
        
        .demo-video a {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 15px 25px;
            background: #ff0000;
            color: white;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .demo-video a:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(255, 0, 0, 0.3);
        }
        
        .social-links {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin: 30px 0;
            flex-wrap: wrap;
        }
        
        .social-link {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--primary);
            color: white;
            font-size: 22px;
            transition: all 0.3s;
            text-decoration: none;
        }
        
        .social-link:hover {
            background: var(--secondary);
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
        }
        
        .copyright {
            text-align: center;
            margin-top: 25px;
            font-size: 14px;
            color: var(--gray);
        }
        
        /* App Container */
        .app-container {
            display: none;
            min-height: 100vh;
            background: #f8fafc;
        }
        
        .app-container.active {
            display: block;
        }
        
        /* Header */
        header {
            background: white;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            position: sticky;
            top: 0;
            z-index: 1000;
            animation: slideDown 0.4s ease;
        }
        
        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 15px;
            cursor: pointer;
        }
        
        .logo-icon {
            width: 48px;
            height: 48px;
            background: var(--gradient);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
            box-shadow: 0 8px 20px rgba(52, 152, 219, 0.2);
        }
        
        .logo h1 {
            font-size: 24px;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 700;
        }
        
        .user-info {
            display: flex;
            align-items: center;
            gap: 20px;
        }
        
        .user-avatar {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: var(--gradient);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 18px;
            box-shadow: 0 8px 20px rgba(52, 152, 219, 0.3);
        }
        
        .user-details {
            text-align: right;
        }
        
        .user-name {
            font-weight: 700;
            color: var(--primary);
            font-size: 16px;
        }
        
        .user-status {
            font-size: 13px;
            color: var(--success);
            font-weight: 600;
        }
        
        .user-menu {
            position: relative;
        }
        
        .user-menu-btn {
            background: none;
            border: none;
            font-size: 24px;
            color: var(--gray);
            cursor: pointer;
            padding: 8px;
            border-radius: 50%;
            transition: all 0.3s;
        }
        
        .user-menu-btn:hover {
            background: #f5f7fa;
            color: var(--primary);
        }
        
        .user-dropdown {
            position: absolute;
            top: 100%;
            right: 0;
            background: white;
            border-radius: 16px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
            min-width: 220px;
            padding: 10px 0;
            display: none;
            z-index: 1000;
            animation: fadeInUp 0.2s ease;
        }
        
        .user-dropdown.active {
            display: block;
        }
        
        .user-dropdown a {
            display: flex;
            align-items: center;
            padding: 14px 24px;
            color: var(--primary);
            text-decoration: none;
            transition: all 0.3s;
            font-weight: 500;
        }
        
        .user-dropdown a:hover {
            background: #f8f9fa;
            color: var(--secondary);
        }
        
        .user-dropdown a i {
            margin-right: 12px;
            width: 20px;
            text-align: center;
        }
        
        /* Navigation */
        nav {
            background: var(--primary);
            position: sticky;
            top: 88px;
            z-index: 999;
        }
        
        .nav-links {
            display: flex;
            list-style: none;
            overflow-x: auto;
            padding: 5px 0;
            scrollbar-width: none;
        }
        
        .nav-links::-webkit-scrollbar {
            display: none;
        }
        
        .nav-links li {
            flex: 0 0 auto;
        }
        
        .nav-links a {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 16px 24px;
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
            font-size: 14px;
            min-width: 90px;
            border-radius: 12px;
            margin: 0 5px;
        }
        
        .nav-links a i {
            font-size: 20px;
            margin-bottom: 8px;
        }
        
        .nav-links a:hover, .nav-links a.active {
            color: white;
            background: rgba(52, 152, 219, 0.2);
        }
        
        .nav-links a.active {
            color: white;
            background: var(--secondary);
            box-shadow: 0 5px 15px rgba(52, 152, 219, 0.3);
        }
        
        /* Main Content */
        .main-content {
            padding: 40px 0;
            min-height: calc(100vh - 200px);
            animation: fadeIn 0.5s ease;
        }
        
        .page {
            display: none;
        }
        
        .page.active {
            display: block;
        }
        
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            flex-wrap: wrap;
            gap: 20px;
        }
        
        .page-title {
            color: var(--primary);
            font-size: 28px;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .page-title i {
            color: var(--secondary);
        }
        
        .page-actions {
            display: flex;
            gap: 12px;
            flex-wrap: wrap;
        }
        
        .btn {
            display: inline-block;
            padding: 12px 24px;
            background: var(--gradient);
            color: white;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            font-size: 15px;
            font-weight: 600;
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
        }
        
        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(52, 152, 219, 0.3);
        }
        
        .btn:active {
            transform: translateY(-1px);
        }
        
        .btn-accent {
            background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
        }
        
        .btn-accent:hover {
            box-shadow: 0 10px 20px rgba(231, 76, 60, 0.3);
        }
        
        .btn-success {
            background: linear-gradient(135deg, #2ecc71 0%, #27ae60 100%);
        }
        
        .btn-warning {
            background: linear-gradient(135deg, #f39c12 0%, #e67e22 100%);
        }
        
        /* Study Plan Card */
        .target-setup-card {
            background: white;
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: var(--shadow);
            border: 1px solid rgba(0, 0, 0, 0.05);
            animation: slideUp 0.5s ease;
        }
        
        .target-setup-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            cursor: pointer;
        }
        
        .target-setup-header h3 {
            color: var(--primary);
            font-size: 20px;
            display: flex;
            align-items: center;
            gap: 12px;
        }
        
        .target-setup-header i {
            color: var(--secondary);
            transition: transform 0.3s;
        }
        
        .target-setup-content {
            display: none;
            animation: fadeIn 0.3s ease;
        }
        
        .target-setup-content.expanded {
            display: block;
        }
        
        .target-form {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            margin-bottom: 25px;
        }
        
        .subject-hours-distribution {
            margin-top: 20px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 12px;
        }
        
        .subject-hour-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 15px;
            padding-bottom: 15px;
            border-bottom: 1px solid #e9ecef;
        }
        
        .subject-hour-item:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
            border-bottom: none;
        }
        
        .hour-input {
            width: 120px;
            padding: 10px;
            border: 2px solid #e1e5e9;
            border-radius: 8px;
            text-align: center;
        }
        
        .target-display {
            background: #f8f9fa;
            border-radius: 16px;
            padding: 25px;
            margin-top: 25px;
        }
        
        .target-display h4 {
            color: var(--primary);
            margin-bottom: 15px;
        }
        
        .target-values {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
        }
        
        .target-value {
            text-align: center;
            padding: 20px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        }
        
        .target-value .value {
            font-size: 32px;
            font-weight: 800;
            color: var(--secondary);
            margin-bottom: 8px;
        }
        
        .target-value .label {
            font-size: 14px;
            color: var(--gray);
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        /* Progress Filters */
        .progress-filters {
            display: flex;
            gap: 12px;
            margin-bottom: 25px;
            flex-wrap: wrap;
        }
        
        .progress-filter {
            padding: 12px 24px;
            background: white;
            border: 2px solid #e9ecef;
            border-radius: 25px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s;
            font-size: 14px;
        }
        
        .progress-filter.active {
            background: var(--secondary);
            color: white;
            border-color: var(--secondary);
            box-shadow: 0 5px 15px rgba(52, 152, 219, 0.2);
        }
        
        /* KPI Cards */
        .kpi-cards {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
        }
        
        .kpi-card {
            background: white;
            border-radius: 20px;
            padding: 25px;
            box-shadow: var(--shadow);
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            transition: all 0.3s;
            border: 1px solid rgba(0, 0, 0, 0.05);
            position: relative;
            overflow: hidden;
        }
        
        .kpi-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background: var(--secondary);
        }
        
        .kpi-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }
        
        .kpi-icon {
            width: 70px;
            height: 70px;
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
            font-size: 28px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        }
        
        .kpi-card h3 {
            font-size: 14px;
            color: var(--gray);
            margin-bottom: 15px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .kpi-value {
            font-size: 32px;
            font-weight: 800;
            color: var(--primary);
            margin-bottom: 8px;
            line-height: 1;
        }
        
        .kpi-subtext {
            font-size: 14px;
            color: var(--gray);
        }
        
        .performance-indicator {
            display: flex;
            align-items: center;
            gap: 8px;
            font-weight: 600;
            font-size: 14px;
        }
        
        .performance-good {
            color: var(--success);
        }
        
        .performance-bad {
            color: var(--accent);
        }
        
        .performance-neutral {
            color: var(--warning);
        }
        
        .kpi-icon.time { background: linear-gradient(135deg, #3498db, #2c3e50); color: white; }
        .kpi-icon.progress { background: linear-gradient(135deg, #2ecc71, #27ae60); color: white; }
        .kpi-icon.subjects { background: linear-gradient(135deg, #9b59b6, #8e44ad); color: white; }
        .kpi-icon.topics { background: linear-gradient(135deg, #f39c12, #e67e22); color: white; }
        .kpi-icon.completion { background: linear-gradient(135deg, #1abc9c, #16a085); color: white; }
        .kpi-icon.targets { background: linear-gradient(135deg, #e74c3c, #c0392b); color: white; }
        .kpi-icon.velocity { background: linear-gradient(135deg, #3498db, #2980b9); color: white; }
        
        /* Charts */
        .chart-container {
            background: white;
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 40px;
            box-shadow: var(--shadow);
            border: 1px solid rgba(0, 0, 0, 0.05);
        }
        
        .chart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
            flex-wrap: wrap;
            gap: 15px;
        }
        
        .chart-title {
            color: var(--primary);
            font-size: 20px;
            font-weight: 600;
        }
        
        .chart-type-selector {
            display: flex;
            gap: 10px;
        }
        
        .chart-type-btn {
            padding: 10px 20px;
            background: #f8f9fa;
            border: 2px solid #e9ecef;
            border-radius: 10px;
            font-weight: 600;
            color: var(--gray);
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .chart-type-btn.active {
            background: var(--secondary);
            color: white;
            border-color: var(--secondary);
        }
        
        .chart-wrapper {
            height: 400px;
            position: relative;
        }
        
        /* Projection Message */
        .projection-message {
            background: linear-gradient(135deg, rgba(241, 196, 15, 0.1) 0%, rgba(243, 156, 18, 0.1) 100%);
            padding: 20px;
            border-radius: 12px;
            margin: 20px 0;
            border: 2px solid rgba(241, 196, 15, 0.2);
            font-size: 16px;
        }
        
        /* Hours Distribution Summary */
        .hours-summary {
            margin-top: 15px;
            padding: 12px;
            background: rgba(46, 204, 113, 0.1);
            border-radius: 8px;
            text-align: center;
            font-weight: 600;
            color: var(--success);
            border: 1px solid rgba(46, 204, 113, 0.2);
        }
        
        .hours-summary.negative {
            background: rgba(231, 76, 60, 0.1);
            color: var(--accent);
            border-color: rgba(231, 76, 60, 0.2);
        }
        
        /* Subjects Page */
        .subject-selector {
            background: white;
            border-radius: 20px;
            padding: 25px;
            margin-bottom: 30px;
            box-shadow: var(--shadow);
        }
        
        .subject-selector label {
            display: block;
            margin-bottom: 15px;
            font-weight: 600;
            color: var(--primary);
            font-size: 16px;
        }
        
        .subject-dropdown {
            width: 100%;
            padding: 16px;
            border: 2px solid #e9ecef;
            border-radius: 12px;
            font-size: 16px;
            background: white;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .subject-dropdown:focus {
            border-color: var(--secondary);
            outline: none;
            box-shadow: 0 0 0 4px rgba(52, 152, 219, 0.1);
        }
        
        .subject-content {
            background: white;
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 40px;
            box-shadow: var(--shadow);
            border: 1px solid rgba(0, 0, 0, 0.05);
        }
        
        .subject-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #f1f3f5;
        }
        
        .subject-header h3 {
            color: var(--primary);
            font-size: 24px;
        }
        
        .subject-hours {
            font-size: 18px;
            color: var(--secondary);
            font-weight: 600;
            background: #f8f9fa;
            padding: 10px 20px;
            border-radius: 12px;
        }
        
        .topic-list {
            margin-top: 20px;
        }
        
        .topic-item {
            border: 1px solid #eee;
            border-radius: 16px;
            margin-bottom: 20px;
            overflow: hidden;
            transition: all 0.3s;
        }
        
        .topic-item:hover {
            border-color: var(--secondary);
            box-shadow: 0 10px 30px rgba(52, 152, 219, 0.1);
        }
        
        .topic-header {
            background: linear-gradient(90deg, #f8f9fa, #f1f3f5);
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .topic-header:hover {
            background: linear-gradient(90deg, #f1f3f5, #e9ecef);
        }
        
        .topic-title {
            font-weight: 700;
            color: var(--primary);
            font-size: 18px;
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .topic-title i {
            transition: transform 0.3s;
        }
        
        .topic-item.expanded .topic-title i {
            transform: rotate(90deg);
        }
        
        .topic-weightage {
            background: var(--secondary);
            color: white;
            padding: 6px 15px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
        }
        
        .topic-priority {
            font-size: 14px;
            padding: 6px 15px;
            border-radius: 20px;
            font-weight: 600;
        }
        
        .priority-A {
            background: rgba(231, 76, 60, 0.1);
            color: var(--accent);
            border: 1px solid rgba(231, 76, 60, 0.2);
        }
        
        .priority-B {
            background: rgba(241, 196, 15, 0.1);
            color: #f1c40f;
            border: 1px solid rgba(241, 196, 15, 0.2);
        }
        
        .priority-C {
            background: rgba(52, 152, 219, 0.1);
            color: var(--secondary);
            border: 1px solid rgba(52, 152, 219, 0.2);
        }
        
        .subtopic-list {
            padding: 0;
            background: white;
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.5s ease;
        }
        
        .topic-item.expanded .subtopic-list {
            padding: 20px;
            max-height: 2000px;
        }
        
        .subtopic-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 1px solid #f5f5f5;
        }
        
        .subtopic-item:last-child {
            border-bottom: none;
        }
        
        .subtopic-info {
            flex: 1;
        }
        
        .subtopic-name {
            font-weight: 600;
            margin-bottom: 8px;
            color: var(--primary);
            font-size: 16px;
        }
        
        .subtopic-details {
            display: flex;
            gap: 20px;
            font-size: 14px;
            color: var(--gray);
        }
        
        .subtopic-actions {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .actual-time-input {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .actual-time-input input {
            width: 100px;
            padding: 10px;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            text-align: center;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .actual-time-input input:focus {
            border-color: var(--secondary);
            outline: none;
        }
        
        .topic-status {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .status-not-started {
            background: rgba(149, 165, 166, 0.1);
            color: var(--gray);
            border: 1px solid rgba(149, 165, 166, 0.2);
        }
        
        .status-inprogress {
            background: rgba(243, 156, 18, 0.1);
            color: var(--warning);
            border: 1px solid rgba(243, 156, 18, 0.2);
        }
        
        .status-completed {
            background: rgba(46, 204, 113, 0.1);
            color: var(--success);
            border: 1px solid rgba(46, 204, 113, 0.2);
        }
        
        /* Planning Page */
        .planning-container {
            display: grid;
            grid-template-columns: 1fr;
            gap: 30px;
        }
        
        @media (min-width: 992px) {
            .planning-container {
                grid-template-columns: 1fr 1fr;
            }
        }
        
        .planning-form {
            background: white;
            border-radius: 20px;
            padding: 0;
            box-shadow: var(--shadow);
            border: 1px solid rgba(0, 0, 0, 0.05);
            overflow: hidden;
            max-height: 0;
            transition: max-height 0.5s ease, padding 0.5s ease;
        }
        
        .planning-form.expanded {
            max-height: 600px;
            padding: 30px;
        }
        
        .planning-form h3 {
            margin-bottom: 25px;
            color: var(--primary);
            font-size: 20px;
        }
        
        .target-list-container {
            background: white;
            border-radius: 20px;
            padding: 30px;
            box-shadow: var(--shadow);
            border: 1px solid rgba(0, 0, 0, 0.05);
        }
        
        .target-list-container h3 {
            margin-bottom: 25px;
            color: var(--primary);
            font-size: 20px;
        }
        
        .search-box {
            margin-bottom: 25px;
        }
        
        .search-box input {
            width: 100%;
            padding: 14px 20px;
            border: 2px solid #e9ecef;
            border-radius: 12px;
            font-size: 16px;
            transition: all 0.3s;
        }
        
        .search-box input:focus {
            border-color: var(--secondary);
            outline: none;
            box-shadow: 0 0 0 4px rgba(52, 152, 219, 0.1);
        }
        
        .target-list {
            max-height: 500px;
            overflow-y: auto;
            padding-right: 10px;
        }
        
        .target-list::-webkit-scrollbar {
            width: 8px;
        }
        
        .target-list::-webkit-scrollbar-track {
            background: #f1f1f1;
            border-radius: 10px;
        }
        
        .target-list::-webkit-scrollbar-thumb {
            background: var(--secondary);
            border-radius: 10px;
        }
        
        .target-item {
            padding: 20px;
            border: 1px solid #eee;
            border-radius: 16px;
            margin-bottom: 20px;
            background: white;
            transition: all 0.3s;
        }
        
        .target-item:hover {
            border-color: var(--secondary);
            box-shadow: 0 10px 30px rgba(52, 152, 219, 0.1);
        }
        
        .target-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }
        
        .target-title {
            font-weight: 700;
            color: var(--primary);
            font-size: 17px;
        }
        
        .target-subject {
            font-size: 14px;
            color: white;
            background: var(--secondary);
            padding: 4px 12px;
            border-radius: 20px;
            display: inline-block;
            margin-bottom: 12px;
        }
        
        .target-details {
            display: flex;
            justify-content: space-between;
            margin-bottom: 15px;
            font-size: 14px;
            color: var(--gray);
        }
        
        .target-time {
            display: flex;
            gap: 20px;
            margin-bottom: 15px;
        }
        
        .target-planned, .target-actual {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .target-actual input {
            width: 80px;
            padding: 8px;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            text-align: center;
        }
        
        .target-actions {
            display: flex;
            gap: 10px;
        }
        
        .complete-target-btn, .delete-target-btn {
            padding: 8px 16px;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .complete-target-btn {
            background: var(--success);
        }
        
        .complete-target-btn:hover {
            background: #27ae60;
        }
        
        .delete-target-btn {
            background: var(--accent);
        }
        
        .delete-target-btn:hover {
            background: #c0392b;
        }
        
        /* History Table */
        .history-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        
        .history-table th {
            background: var(--primary);
            color: white;
            padding: 15px;
            text-align: left;
        }
        
        .history-table td {
            padding: 12px 15px;
            border-bottom: 1px solid #eee;
        }
        
        .history-table tr:hover {
            background: #f8f9fa;
        }
        
        .achievement-cell {
            font-weight: bold;
        }
        
        .achievement-high {
            color: var(--success);
        }
        
        .achievement-medium {
            color: var(--warning);
        }
        
        .achievement-low {
            color: var(--accent);
        }
        
        /* Videos & Resources Pages */
        .resources-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            flex-wrap: wrap;
            gap: 20px;
        }
        
        .add-resource-toggle {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 24px;
            background: white;
            border: 2px solid var(--secondary);
            color: var(--secondary);
            border-radius: 12px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s;
        }
        
        .add-resource-toggle:hover {
            background: var(--secondary);
            color: white;
            transform: translateY(-2px);
        }
        
        .add-resource-toggle i {
            transition: transform 0.3s;
        }
        
        .add-resource-toggle.expanded i {
            transform: rotate(180deg);
        }
        
        .resources-container {
            display: grid;
            grid-template-columns: 1fr;
            gap: 40px;
        }
        
        @media (min-width: 992px) {
            .resources-container {
                grid-template-columns: 1fr 1fr;
            }
        }
        
        .add-resource-form {
            background: white;
            border-radius: 20px;
            padding: 0;
            box-shadow: var(--shadow);
            border: 1px solid rgba(0, 0, 0, 0.05);
            overflow: hidden;
            max-height: 0;
            transition: max-height 0.5s ease, padding 0.5s ease;
        }
        
        .add-resource-form.expanded {
            max-height: 600px;
            padding: 30px;
        }
        
        .resource-list-container {
            background: white;
            border-radius: 20px;
            padding: 30px;
            box-shadow: var(--shadow);
            border: 1px solid rgba(0, 0, 0, 0.05);
        }
        
        .resource-list {
            max-height: 600px;
            overflow-y: auto;
            padding-right: 10px;
        }
        
        .resource-item {
            padding: 20px;
            border: 1px solid #eee;
            border-radius: 16px;
            margin-bottom: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s;
            background: white;
        }
        
        .resource-item:hover {
            border-color: var(--secondary);
            box-shadow: 0 10px 30px rgba(52, 152, 219, 0.1);
            transform: translateY(-2px);
        }
        
        .resource-info {
            flex: 1;
            min-width: 0;
        }
        
        .resource-info h4 {
            margin-bottom: 10px;
            color: var(--primary);
            font-size: 17px;
            font-weight: 600;
        }
        
        .resource-subject {
            font-size: 14px;
            color: white;
            background: var(--secondary);
            padding: 4px 12px;
            border-radius: 20px;
            display: inline-block;
            margin-bottom: 12px;
            font-weight: 500;
        }
        
        .resource-author {
            font-size: 14px;
            color: var(--gray);
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .resource-actions {
            display: flex;
            gap: 12px;
        }
        
        .resource-btn {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #f8f9fa;
            color: var(--primary);
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            border: none;
            font-size: 18px;
        }
        
        .resource-btn:hover {
            background: var(--secondary);
            color: white;
            transform: translateY(-2px);
        }
        
        /* Settings Modal */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(5px);
            z-index: 2000;
            align-items: center;
            justify-content: center;
            padding: 20px;
            animation: fadeIn 0.3s ease;
        }
        
        .modal.active {
            display: flex;
        }
        
        .modal-content {
            background: white;
            border-radius: 24px;
            width: 100%;
            max-width: 500px;
            max-height: 90vh;
            overflow-y: auto;
            animation: slideUp 0.4s ease;
        }
        
        .modal-header {
            padding: 30px;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .modal-header h3 {
            color: var(--primary);
            font-size: 24px;
        }
        
        .modal-close {
            background: none;
            border: none;
            font-size: 28px;
            color: var(--gray);
            cursor: pointer;
            transition: all 0.3s;
        }
        
        .modal-close:hover {
            color: var(--accent);
        }
        
        .modal-body {
            padding: 30px;
        }
        
        .delete-section {
            margin-bottom: 40px;
        }
        
        .delete-section h4 {
            margin-bottom: 20px;
            color: var(--primary);
            font-size: 18px;
        }
        
        .resource-checkboxes {
            max-height: 200px;
            overflow-y: auto;
            margin-bottom: 25px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 16px;
        }
        
        .checkbox-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 12px;
            border-bottom: 1px solid #eee;
        }
        
        .checkbox-item:last-child {
            border-bottom: none;
        }
        
        .password-input {
            position: relative;
            margin-top: 25px;
        }
        
        .password-input input {
            width: 100%;
            padding: 16px;
            border: 2px solid #e9ecef;
            border-radius: 12px;
            font-size: 16px;
            transition: all 0.3s;
        }
        
        /* Delete Entry Modal */
        .delete-entry-modal {
            max-width: 600px;
        }
        
        .entry-type-selector {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }
        
        .entry-type-btn {
            flex: 1;
            padding: 12px;
            background: #f8f9fa;
            border: 2px solid #e9ecef;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 600;
            color: var(--gray);
            text-align: center;
            transition: all 0.3s;
        }
        
        .entry-type-btn.active {
            background: var(--secondary);
            color: white;
            border-color: var(--secondary);
        }
        
        /* Footer */
        footer {
            background: var(--primary);
            color: white;
            padding: 50px 0 30px;
            text-align: center;
            margin-top: 60px;
        }
        
        .footer-content {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 30px;
        }
        
        .footer-social-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 10px 0;
            flex-wrap: wrap;
        }
        
        .footer-copyright {
            margin-top: 25px;
            font-size: 15px;
            color: rgba(255, 255, 255, 0.7);
        }
        
        /* Toast Notification */
        .toast {
            position: fixed;
            bottom: 40px;
            right: 40px;
            background: white;
            border-radius: 16px;
            padding: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
            display: flex;
            align-items: center;
            gap: 20px;
            z-index: 3000;
            transform: translateX(150%);
            transition: transform 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            border-left: 5px solid var(--success);
        }
        
        .toast.show {
            transform: translateX(0);
        }
        
        .toast i {
            font-size: 28px;
            color: var(--success);
        }
        
        .toast.error {
            border-left-color: var(--accent);
        }
        
        .toast.error i {
            color: var(--accent);
        }
        
        .toast.warning {
            border-left-color: var(--warning);
        }
        
        .toast.warning i {
            color: var(--warning);
        }
        
        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: var(--gray);
        }
        
        .empty-state i {
            font-size: 80px;
            margin-bottom: 25px;
            color: #e9ecef;
            opacity: 0.5;
        }
        
        .empty-state h3 {
            margin-bottom: 15px;
            color: var(--primary);
            font-size: 22px;
        }
        
        .empty-state p {
            font-size: 16px;
            max-width: 400px;
            margin: 0 auto;
        }
        
        /* Data Management */
        .export-import-section {
            margin-top: 40px;
            padding: 30px;
            background: white;
            border-radius: 20px;
            box-shadow: var(--shadow);
        }
        
        .data-actions {
            display: flex;
            gap: 15px;
            margin-top: 20px;
            flex-wrap: wrap;
        }
        
        /* Animations */
        @keyframes slideDown {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        @keyframes slideUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            .license-box {
                padding: 30px;
                margin: 20px;
            }
            
            .header-content {
                flex-direction: column;
                gap: 20px;
            }
            
            .logo h1 {
                font-size: 22px;
            }
            
            .nav-links a {
                padding: 14px 20px;
                min-width: 80px;
                font-size: 13px;
            }
            
            .nav-links a i {
                font-size: 18px;
            }
            
            .kpi-cards {
                grid-template-columns: 1fr;
            }
            
            .page-header {
                flex-direction: column;
                align-items: flex-start;
            }
            
            .page-actions {
                width: 100%;
            }
            
            .modal-content {
                margin: 10px;
            }
            
            .toast {
                left: 20px;
                right: 20px;
                bottom: 20px;
            }
        }
        
        @media (max-width: 480px) {
            .target-form {
                grid-template-columns: 1fr;
            }
            
            .chart-type-selector {
                width: 100%;
            }
            
            .chart-type-btn {
                flex: 1;
                text-align: center;
                font-size: 13px;
                padding: 8px 12px;
            }
            
            .data-actions {
                flex-direction: column;
            }
            
            .target-time {
                flex-direction: column;
                gap: 10px;
            }
        }
        
        /* Velocity KPI Card */
        .velocity-card {
            grid-column: span 2;
        }
        
        @media (max-width: 768px) {
            .velocity-card {
                grid-column: span 1;
            }
        }
        
        /* Overdue Status */
        .status-overdue {
            background: rgba(231, 76, 60, 0.1);
            color: var(--accent);
            border: 1px solid rgba(231, 76, 60, 0.2);
        }
    </style>
</head>
<body>
    <!-- Enhanced License Screen -->
<div class="license-container" id="license-container">
    <div class="license-box">
        <div class="license-header">
            <div class="license-logo">
                <i class="fas fa-graduation-cap"></i>
            </div>
            <h1>CA Final Pro Tracker</h1>
            <p>Professional Offline Study Management System</p>
        </div>
        
        <div class="license-status-indicator" id="license-status-indicator" style="display: none;">
            <div class="license-status-message">
                <i class="fas fa-info-circle"></i>
                <span id="license-status-text">Checking license...</span>
            </div>
        </div>
        
        <div class="form-group">
            <label for="license-key">
                <i class="fas fa-key"></i> License Key
                <span style="font-size: 12px; color: var(--gray); margin-left: 10px;">Format: CA-PRO-PREMIUM-XXXXXX</span>
            </label>
            <div class="license-input-group">
                <input type="text" id="license-key" placeholder="Enter your premium license key">
                <button class="license-scan-btn" id="scan-license-btn" title="Scan from image">
                    <i class="fas fa-camera"></i>
                </button>
            </div>
            <div class="license-format-hint">
                <small>License should look like: CA-PRO-PREMIUM-eNcRyPtEdCoDeHeRe</small>
            </div>
        </div>
        
        <button class="btn license-activate-btn" id="activate-btn">
            <i class="fas fa-unlock-alt"></i> Activate Premium License
        </button>
        
        <div class="license-info-box" style="margin-top: 25px; padding: 15px; background: rgba(52, 152, 219, 0.1); border-radius: 12px;">
            <h4 style="color: var(--primary); margin-bottom: 10px; font-size: 16px;">
                <i class="fas fa-shield-alt"></i> Premium Features Included:
            </h4>
            <ul style="color: var(--gray); font-size: 14px; padding-left: 20px;">
                <li>Advanced Analytics Dashboard</li>
                <li>Smart Pace Calculations</li>
                <li>Daily Notifications</li>
                <li>Unlimited Data Backup</li>
                <li>Priority Support</li>
            </ul>
        </div>
        
        <div class="demo-video">
            <a href="https://youtube.com/@__satyamkumar?si=yTdGDrWa-VE2_R5m" target="_blank" class="demo-video-link">
                <i class="fab fa-youtube"></i> Watch Demo & Tutorial
            </a>
        </div>
        
        <div class="support-section" style="margin: 25px 0; text-align: center;">
            <p style="color: var(--gray); font-size: 14px; margin-bottom: 15px;">
                Need help with license?
            </p>
            <a href="https://wa.me/919142300129" target="_blank" class="whatsapp-support-btn">
                <i class="fab fa-whatsapp"></i> Contact on WhatsApp
            </a>
        </div>
        
        <div class="social-links">
            <a href="https://satqm.github.io/Portfolio-/" target="_blank" class="social-link" title="Portfolio">
                <i class="fas fa-user-tie"></i>
            </a>
            <a href="https://www.linkedin.com/in/satyam-kumar5286o" target="_blank" class="social-link" title="LinkedIn">
                <i class="fab fa-linkedin-in"></i>
            </a>
            <a href="https://youtube.com/@__satyamkumar?si=yTdGDrWa-VE2_R5m" target="_blank" class="social-link" title="YouTube">
                <i class="fab fa-youtube"></i>
            </a>
            <a href="https://www.instagram.com/satyqm2.o?igsh=bmQ0aThzamRmMHli" target="_blank" class="social-link" title="Instagram">
                <i class="fab fa-instagram"></i>
            </a>
        </div>
        
        <div class="copyright">
            <p>© 2026 CA Final Pro Tracker. Professional Version</p>
            <p style="font-size: 12px; color: var(--gray); margin-top: 5px;">Designed by Satyam Kumar</p>
        </div>
    </div>
</div>

    
    <!-- App Container -->
    <div class="app-container" id="app-container">
        <header>
            <div class="container header-content">
                <div class="logo" id="home-btn">
                    <div class="logo-icon">
                        <i class="fas fa-book-clock"></i>
                    </div>
                    <h1>CA Final Pro Tracker</h1>
                </div>
                <div class="user-info">
                    <div class="user-avatar" id="user-avatar">SK</div>
                    <div class="user-details">
                        <div class="user-name" id="user-name">Satyam Kumar</div>
                        <div class="user-status">Premium Member</div>
                    </div>
                    <div class="user-menu">
                        <button class="user-menu-btn" id="user-menu-btn">
                            <i class="fas fa-ellipsis-v"></i>
                        </button>
                        <div class="user-dropdown" id="user-dropdown">
                            <a href="#" id="export-btn"><i class="fas fa-download"></i> Export Data</a>
                            <a href="#" id="import-btn"><i class="fas fa-upload"></i> Import Data</a>
                            <a href="#" id="delete-entry-btn"><i class="fas fa-trash-alt"></i> Delete Entry</a>
                            <a href="#" id="settings-btn"><i class="fas fa-cog"></i> Settings</a>
                            <a href="#" id="reset-app-btn"><i class="fas fa-redo"></i> Reset App</a>
                        </div>
                    </div>
                </div>
            </div>
            
            <nav>
                <div class="container">
                    <ul class="nav-links">
                        <li><a href="#" class="nav-link active" data-page="dashboard"><i class="fas fa-tachometer-alt"></i> Dashboard</a></li>
                        <li><a href="#" class="nav-link" data-page="subjects"><i class="fas fa-book"></i> Subjects</a></li>
                        <li><a href="#" class="nav-link" data-page="planning"><i class="fas fa-calendar-check"></i> Planning</a></li>
                        <li><a href="#" class="nav-link" data-page="videos"><i class="fas fa-video"></i> Videos</a></li>
                        <li><a href="#" class="nav-link" data-page="resources"><i class="fas fa-sticky-note"></i> Resources</a></li>
                    </ul>
                </div>
            </nav>
        </header>
        
        <main class="main-content">
            <div class="container">
                <!-- Dashboard Page -->
                <div class="page active" id="dashboard-page">
                    <div class="page-header">
                        <h2 class="page-title"><i class="fas fa-tachometer-alt"></i> Study Dashboard</h2>
                        <div class="page-actions">
                            <button class="btn" id="update-target-btn">
                                <i class="fas fa-calendar-alt"></i> Update Study Plan
                            </button>
                        </div>
                    </div>
                    
                    <!-- Study Plan Card -->
                    <div class="target-setup-card" id="target-setup-card">
                        <div class="target-setup-header" id="target-toggle">
                            <h3><i class="fas fa-bullseye"></i> Your Study Plan</h3>
                            <i class="fas fa-chevron-down"></i>
                        </div>
                        <div class="target-setup-content" id="target-content">
                            <div class="target-form">
                                <div class="form-group">
                                    <label for="days-to-exam">Days until exam</label>
                                    <input type="number" id="days-to-exam" min="1" max="365" value="180" class="form-control">
                                </div>
                                <div class="form-group">
                                    <label for="daily-hours">Daily study hours</label>
                                    <input type="number" id="daily-hours" min="1" max="24" value="4" step="0.5" class="form-control">
                                </div>
                                <div class="form-group">
                                    <label for="start-date">Start date</label>
                                    <input type="date" id="start-date" class="form-control">
                                </div>
                            </div>
                            <button class="btn" id="calculate-plan-btn">
                                <i class="fas fa-calculator"></i> Calculate Plan
                            </button>
                            
                            <div class="subject-hours-distribution" id="subject-hours-distribution" style="display: none;">
                                <h4>Customize Subject Hours</h4>
                                <div id="subject-hours-list">
                                    <!-- Will be populated by JavaScript -->
                                </div>
                                <div id="hours-summary" class="hours-summary"></div>
                            </div>
                            
                            <div class="target-display" id="target-display" style="display: none;">
                                <h4><i class="fas fa-chart-line"></i> Your Study Plan Summary</h4>
                                <div class="target-values">
                                    <div class="target-value">
                                        <div class="value" id="total-days">0</div>
                                        <div class="label">Total Days</div>
                                    </div>
                                    <div class="target-value">
                                        <div class="value" id="total-hours">0</div>
                                        <div class="label">Total Hours</div>
                                    </div>
                                    <div class="target-value">
                                        <div class="value" id="daily-target">0</div>
                                        <div class="label">Hours/Day</div>
                                    </div>
                                    <div class="target-value">
                                        <div class="value" id="exam-date">-</div>
                                        <div class="label">Exam Date</div>
                                    </div>
                                </div>
                                <button class="btn btn-accent" id="save-plan-btn" style="margin-top: 20px;">
                                    <i class="fas fa-save"></i> Save This Plan
                                </button>
                                <button class="btn btn-warning" id="reset-plan-btn" style="margin-top: 10px;">
                                    <i class="fas fa-redo"></i> Reset to Default
                                </button>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Smart Projection -->
                    <div class="projection-message" id="projection-message">
                        <i class="fas fa-chart-line"></i>
                        <span id="projection-text">Set up your study plan to see projections...</span>
                    </div>
                    
                    <!-- Progress Filters -->
                    <div class="progress-filters">
                        <div class="progress-filter active" data-filter="all">All Subjects</div>
                        <div class="progress-filter" data-filter="paper1">Paper 1: FR</div>
                        <div class="progress-filter" data-filter="paper2">Paper 2: AFM</div>
                        <div class="progress-filter" data-filter="paper3">Paper 3: Audit</div>
                        <div class="progress-filter" data-filter="paper4">Paper 4: DT</div>
                        <div class="progress-filter" data-filter="paper5">Paper 5: IDT</div>
                        <div class="progress-filter" data-filter="paper6">Paper 6: IBS</div>
                    </div>
                    
                    <!-- KPI Cards -->
                    <div class="kpi-cards">
                        <div class="kpi-card">
                            <div class="kpi-icon time">
                                <i class="fas fa-clock"></i>
                            </div>
                            <h3>Study Time</h3>
                            <div class="kpi-value" id="actual-hours">0</div>
                            <div class="kpi-subtext">of <span id="planned-hours">0</span> planned hours</div>
                        </div>
                        
                        <div class="kpi-card">
                            <div class="kpi-icon progress">
                                <i class="fas fa-chart-pie"></i>
                            </div>
                            <h3>Course Progress</h3>
                            <div class="kpi-value" id="overall-progress">0%</div>
                            <div class="kpi-subtext">Overall Completion</div>
                        </div>
                        
                        <div class="kpi-card">
                            <div class="kpi-icon subjects">
                                <i class="fas fa-book-open"></i>
                            </div>
                            <h3>Subjects</h3>
                            <div class="kpi-value" id="subjects-completed">0</div>
                            <div class="kpi-subtext">of 6 Subjects</div>
                        </div>
                        
                        <div class="kpi-card">
                            <div class="kpi-icon topics">
                                <i class="fas fa-tasks"></i>
                            </div>
                            <h3>Topics</h3>
                            <div class="kpi-value" id="topics-completed">0</div>
                            <div class="kpi-subtext">of <span id="total-topics">58</span> Topics</div>
                        </div>
                        
                        <div class="kpi-card">
                            <div class="kpi-icon completion">
                                <i class="fas fa-flag-checkered"></i>
                            </div>
                            <h3>Plan Completion</h3>
                            <div class="kpi-value" id="plan-completion">0%</div>
                            <div class="kpi-subtext">Hours Completed</div>
                        </div>
                        
                        <div class="kpi-card">
                            <div class="kpi-icon targets">
                                <i class="fas fa-bullseye"></i>
                            </div>
                            <h3>Targets</h3>
                            <div class="kpi-value" id="targets-achieved">0/0</div>
                            <div class="kpi-subtext">Achieved / Total</div>
                        </div>
                        
                        <div class="kpi-card velocity-card">
                            <div class="kpi-icon velocity">
                                <i class="fas fa-tachometer-alt"></i>
                            </div>
                            <h3>Current Pace</h3>
                            <div class="kpi-value" id="current-pace">0 hrs/day</div>
                            <div class="kpi-subtext" id="pace-status">Calculating...</div>
                        </div>
                    </div>
                    
                    <!-- Performance Indicator -->
                    <div class="target-setup-card" style="margin-bottom: 20px;">
                        <div class="target-setup-header">
                            <h3><i class="fas fa-trophy"></i> Performance Analysis</h3>
                        </div>
                        <div class="target-setup-content expanded">
                            <div class="target-values">
                                <div class="target-value">
                                    <div class="value" id="success-rate">0%</div>
                                    <div class="label">Target Success</div>
                                </div>
                                <div class="target-value">
                                    <div class="value">
                                        <span id="performance-indicator" class="performance-indicator">
                                            <i class="fas fa-arrow-up performance-good"></i>
                                            <span>On Track</span>
                                        </span>
                                    </div>
                                    <div class="label">Performance</div>
                                </div>
                                <div class="target-value">
                                    <div class="value" id="daily-efficiency">0%</div>
                                    <div class="label">Daily Efficiency</div>
                                </div>
                                <div class="target-value">
                                    <div class="value" id="streak-days">0</div>
                                    <div class="label">Day Streak</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Charts -->
                    <div class="chart-container">
                        <div class="chart-header">
                            <h3 class="chart-title">Progress Analysis</h3>
                            <div class="chart-type-selector">
                                <button class="chart-type-btn active" data-chart="subject">Subject View</button>
                                <button class="chart-type-btn" data-chart="time">Time Analysis</button>
                                <button class="chart-type-btn" data-chart="targets">Target Progress</button>
                            </div>
                        </div>
                        <div class="chart-wrapper">
                            <canvas id="progress-chart"></canvas>
                        </div>
                    </div>
                    
                    <!-- Data Management -->
                    <div class="export-import-section">
                        <h3><i class="fas fa-database"></i> Data Management</h3>
                        <p>Backup your study data or restore from previous backup</p>
                        <div class="data-actions">
                            <button class="btn" id="backup-all-btn">
                                <i class="fas fa-file-export"></i> Backup All Data
                            </button>
                            <button class="btn btn-accent" id="restore-backup-btn">
                                <i class="fas fa-file-import"></i> Restore Backup
                            </button>
                            <input type="file" id="backup-file" accept=".json" style="display: none;">
                        </div>
                        <div id="backup-message" class="license-message" style="margin-top: 15px;"></div>
                    </div>
                </div>
                
                <!-- Subjects Page -->
                <div class="page" id="subjects-page">
                    <div class="page-header">
                        <h2 class="page-title"><i class="fas fa-book"></i> Subjects & Topics</h2>
                        <button class="btn btn-accent" id="save-all-progress-btn">
                            <i class="fas fa-save"></i> Save All Progress
                        </button>
                    </div>
                    
                    <div class="subject-selector">
                        <label for="subject-select">Select Subject:</label>
                        <select id="subject-select" class="subject-dropdown">
                            <option value="all">All Subjects</option>
                            <option value="paper1">Paper 1: Financial Reporting (FR)</option>
                            <option value="paper2">Paper 2: Advanced Financial Management (AFM)</option>
                            <option value="paper3">Paper 3: Auditing and Assurance (Audit)</option>
                            <option value="paper4">Paper 4: Direct Tax (DT)</option>
                            <option value="paper5">Paper 5: Indirect Tax (IDT)</option>
                            <option value="paper6">Paper 6: Integrated Business Solutions (IBS)</option>
                        </select>
                    </div>
                    
                    <div id="subject-content">
                        <!-- Subject content will be loaded here dynamically -->
                    </div>
                </div>
                
                <!-- Planning Page -->
                <div class="page" id="planning-page">
                    <div class="page-header">
                        <h2 class="page-title"><i class="fas fa-calendar-check"></i> Daily Planning</h2>
                        <div class="page-actions">
                            <div class="add-resource-toggle" id="planning-toggle">
                                <i class="fas fa-plus"></i>
                                <span>Add Target</span>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Add Target Form -->
                    <div class="planning-form" id="planning-form">
                        <h3>Set New Target</h3>
                        
                        <div class="form-group">
                            <label for="planning-subject">Subject</label>
                            <select id="planning-subject" class="subject-dropdown">
                                <option value="">Select Subject</option>
                                <option value="paper1">Paper 1: FR</option>
                                <option value="paper2">Paper 2: AFM</option>
                                <option value="paper3">Paper 3: Audit</option>
                                <option value="paper4">Paper 4: DT</option>
                                <option value="paper5">Paper 5: IDT</option>
                                <option value="paper6">Paper 6: IBS</option>
                                <option value="general">General</option>
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label for="planning-topic">Topic</label>
                            <select id="planning-topic" class="subject-dropdown">
                                <option value="">Select Topic</option>
                                <!-- Topics will be loaded dynamically -->
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label for="planning-subtopic">Subtopic (Optional)</label>
                            <select id="planning-subtopic" class="subject-dropdown">
                                <option value="">Select Subtopic</option>
                                <!-- Subtopics will be loaded dynamically -->
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label for="target-date">Target Date</label>
                            <input type="date" id="target-date" class="subject-dropdown">
                        </div>
                        
                        <div class="form-group">
                            <label for="planned-hours-input">Planned Hours</label>
                            <input type="number" id="planned-hours-input" min="0.5" max="24" step="0.5" value="2" placeholder="Enter planned hours">
                        </div>
                        
                        <button class="btn" id="save-target-btn">
                            <i class="fas fa-save"></i> Save Target
                        </button>
                        <div id="target-message" class="license-message" style="margin-top: 20px;"></div>
                    </div>
                    
                    <div class="planning-container">
                        <!-- Live Targets -->
                        <div class="target-list-container">
                            <h3><i class="fas fa-bolt"></i> Live Targets</h3>
                            <div class="target-filters" style="margin-bottom: 20px; display: flex; gap: 10px;">
                                <select id="target-status-filter" class="subject-dropdown" style="flex: 1;">
                                    <option value="active">Active Targets</option>
                                    <option value="all">All Targets</option>
                                    <option value="overdue">Overdue Targets</option>
                                </select>
                                <select id="target-subject-filter" class="subject-dropdown" style="flex: 1;">
                                    <option value="all">All Subjects</option>
                                    <option value="paper1">Paper 1: FR</option>
                                    <option value="paper2">Paper 2: AFM</option>
                                    <option value="paper3">Paper 3: Audit</option>
                                    <option value="paper4">Paper 4: DT</option>
                                    <option value="paper5">Paper 5: IDT</option>
                                    <option value="paper6">Paper 6: IBS</option>
                                </select>
                            </div>
                            <div id="targets-list" class="target-list">
                                <!-- Targets will be loaded here dynamically -->
                            </div>
                        </div>
                        
                        <!-- Planning Stats -->
                        <div class="target-list-container">
                            <h3><i class="fas fa-chart-bar"></i> Planning Statistics</h3>
                            <div class="target-values" style="grid-template-columns: repeat(2, 1fr);">
                                <div class="target-value">
                                    <div class="value" id="total-planned-targets">0</div>
                                    <div class="label">Total Targets</div>
                                </div>
                                <div class="target-value">
                                    <div class="value" id="completed-targets">0</div>
                                    <div class="label">Completed</div>
                                </div>
                                <div class="target-value">
                                    <div class="value" id="successful-targets">0</div>
                                    <div class="label">Successful</div>
                                </div>
                                <div class="target-value">
                                    <div class="value" id="overdue-targets">0</div>
                                    <div class="label">Overdue</div>
                                </div>
                            </div>
                            <div style="margin-top: 20px; padding: 15px; background: #f8f9fa; border-radius: 12px;">
                                <h4 style="margin-bottom: 10px; color: var(--primary);">Success Rate Analysis</h4>
                                <div class="progress-bar" style="height: 20px; background: #e9ecef; border-radius: 10px; overflow: hidden;">
                                    <div class="progress-fill" id="success-rate-bar" style="height: 100%; background: var(--success); width: 0%;"></div>
                                </div>
                                <div style="display: flex; justify-content: space-between; margin-top: 10px; font-size: 14px;">
                                    <span>Target Completion Success</span>
                                    <span id="success-rate-text">0%</span>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Target History -->
                    <div class="target-list-container" style="margin-top: 30px;">
                        <h3><i class="fas fa-history"></i> Target History</h3>
                        <div class="search-box">
                            <input type="text" id="search-history" placeholder="Search historical targets...">
                        </div>
                        <div style="overflow-x: auto;">
                            <table class="history-table" id="history-table">
                                <thead>
                                    <tr>
                                        <th>Date</th>
                                        <th>Target</th>
                                        <th>Subject</th>
                                        <th>Planned</th>
                                        <th>Actual</th>
                                        <th>Achievement %</th>
                                        <th>Status</th>
                                    </tr>
                                </thead>
                                <tbody id="history-tbody">
                                    <!-- Historical data loaded here -->
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                
                <!-- Videos Page -->
                <div class="page" id="videos-page">
                    <div class="page-header">
                        <h2 class="page-title"><i class="fas fa-video"></i> Video Resources</h2>
                        <div class="page-actions">
                            <div class="add-resource-toggle" id="video-toggle">
                                <i class="fas fa-plus"></i>
                                <span>Add Video</span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="resources-container">
                        <!-- Add Video Form -->
                        <div class="add-resource-form" id="video-form">
                            <h3>Add New Video</h3>
                            <div class="form-group">
                                <label for="video-title">Video Title</label>
                                <input type="text" id="video-title" placeholder="Enter video title">
                            </div>
                            <div class="form-group">
                                <label for="video-subject">Subject</label>
                                <select id="video-subject" class="subject-dropdown">
                                    <option value="paper1">Paper 1: FR</option>
                                    <option value="paper2">Paper 2: AFM</option>
                                    <option value="paper3">Paper 3: Audit</option>
                                    <option value="paper4">Paper 4: DT</option>
                                    <option value="paper5">Paper 5: IDT</option>
                                    <option value="paper6">Paper 6: IBS</option>
                                    <option value="general">General</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="video-link">Video URL (YouTube, Vimeo, etc.)</label>
                                <input type="text" id="video-link" placeholder="Paste video link here">
                            </div>
                            <button class="btn" id="add-video-btn">Add Video</button>
                            <div id="video-message" class="license-message" style="margin-top: 20px;"></div>
                        </div>
                        
                        <!-- Video List -->
                        <div class="resource-list-container">
                            <h3>Video Library</h3>
                            <div class="search-box">
                                <input type="text" id="search-videos" placeholder="Search videos...">
                            </div>
                            <div id="videos-list" class="resource-list">
                                <!-- Videos loaded here -->
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Resources Page -->
                <div class="page" id="resources-page">
                    <div class="page-header">
                        <h2 class="page-title"><i class="fas fa-sticky-note"></i> Study Resources</h2>
                        <div class="page-actions">
                            <div class="add-resource-toggle" id="resources-toggle">
                                <i class="fas fa-plus"></i>
                                <span>Add Resource</span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="resources-container">
                        <!-- Add Resource Form -->
                        <div class="add-resource-form" id="resources-form">
                            <h3>Add New Resource</h3>
                            <div class="form-group">
                                <label for="resources-title">Resource Title</label>
                                <input type="text" id="resources-title" placeholder="Enter resource title">
                            </div>
                            <div class="form-group">
                                <label for="resources-subject">Subject</label>
                                <select id="resources-subject" class="subject-dropdown">
                                    <option value="paper1">Paper 1: FR</option>
                                    <option value="paper2">Paper 2: AFM</option>
                                    <option value="paper3">Paper 3: Audit</option>
                                    <option value="paper4">Paper 4: DT</option>
                                    <option value="paper5">Paper 5: IDT</option>
                                    <option value="paper6">Paper 6: IBS</option>
                                    <option value="general">General</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="resources-type">Resource Type</label>
                                <select id="resources-type" class="subject-dropdown">
                                    <option value="notes">Notes</option>
                                    <option value="question_paper">Question Paper</option>
                                    <option value="mcq">MCQs</option>
                                    <option value="summary">Summary</option>
                                    <option value="formulas">Formulas</option>
                                    <option value="other">Other</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="resources-link">Resource Link (Google Drive, Dropbox, etc.)</label>
                                <input type="text" id="resources-link" placeholder="Paste resource link here">
                            </div>
                            <button class="btn" id="add-resources-btn">Add Resource</button>
                            <div id="resources-message" class="license-message" style="margin-top: 20px;"></div>
                        </div>
                        
                        <!-- Resource List -->
                        <div class="resource-list-container">
                            <h3>Resources Collection</h3>
                            <div class="search-box">
                                <input type="text" id="search-resources" placeholder="Search resources...">
                            </div>
                            <div id="resources-list" class="resource-list">
                                <!-- Resources loaded here -->
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
        
        <footer>
            <div class="container">
                <div class="footer-content">
                    <h3>CA Final Pro Tracker</h3>
                    <div class="footer-social-links">
                        <a href="https://satqm.github.io/Portfolio-/" target="_blank" class="social-link">
                            <i class="fas fa-user-tie"></i>
                        </a>
                        <a href="https://wa.me/919142300129" target="_blank" class="social-link">
                            <i class="fab fa-whatsapp"></i>
                        </a>
                        <a href="https://www.linkedin.com/in/satyam-kumar5286o" target="_blank" class="social-link">
                            <i class="fab fa-linkedin-in"></i>
                        </a>
                        <a href="https://youtube.com/@__satyamkumar?si=yTdGDrWa-VE2_R5m" target="_blank" class="social-link">
                            <i class="fab fa-youtube"></i>
                        </a>
                        <a href="https://www.instagram.com/satyqm2.o?igsh=bmQ0aThzamRmMHli" target="_blank" class="social-link">
                            <i class="fab fa-instagram"></i>
                        </a>
                    </div>
                    <div class="footer-copyright">Professional Offline Version • © 2026 Satyam Kumar</div>
                </div>
            </div>
        </footer>
    </div>
    
    <!-- Delete Entry Modal -->
    <div class="modal" id="delete-entry-modal">
        <div class="modal-content delete-entry-modal">
            <div class="modal-header">
                <h3>Delete Entry</h3>
                <button class="modal-close" id="delete-entry-close">&times;</button>
            </div>
            <div class="modal-body">
                <div class="entry-type-selector">
                    <button class="entry-type-btn active" data-type="targets">Targets</button>
                    <button class="entry-type-btn" data-type="videos">Videos</button>
                    <button class="entry-type-btn" data-type="resources">Resources</button>
                </div>
                
                <div id="entry-list-container" class="resource-checkboxes">
                    <!-- Entries will be loaded here -->
                </div>
                
                <div class="password-input">
                    <input type="password" id="delete-password" placeholder="Enter password (R@dha)">
                </div>
                
                <button class="btn btn-accent" id="confirm-delete-btn" style="margin-top: 20px; width: 100%;">
                    <i class="fas fa-trash"></i> Delete Selected Entries
                </button>
                
                <div id="delete-message" class="license-message" style="margin-top: 20px;"></div>
            </div>
        </div>
    </div>
    
    <!-- Import Modal -->
    <div class="modal" id="import-modal">
        <div class="modal-content">
            <div class="modal-header">
                <h3>Import Backup Data</h3>
                <button class="modal-close" id="import-close">&times;</button>
            </div>
            <div class="modal-body">
                <div class="form-group">
                    <label for="backup-upload">Select Backup File (.json)</label>
                    <input type="file" id="backup-upload" accept=".json" class="subject-dropdown">
                </div>
                <button class="btn btn-accent" id="process-backup-btn">
                    <i class="fas fa-upload"></i> Process Backup
                </button>
                <div id="import-message" class="license-message" style="margin-top: 20px;"></div>
            </div>
        </div>
    </div>
    
    <!-- Toast Notification -->
    <div class="toast" id="toast">
        <i class="fas fa-check-circle"></i>
        <div>
            <h4 id="toast-title">Success!</h4>
            <p id="toast-message">Operation completed successfully.</p>
        </div>
    </div>


    <script>
  // ENHANCED LICENSE SYSTEM
// ====================
const MASTER_KEY = "S@tyamCA2026@Premium";
const BACKUP_KEY = "RadhaKrishna@2026";

class EnhancedLicenseSystem {
    static generateLicense(name, days = 365, type = "premium") {
        const expiryDate = new Date();
        expiryDate.setDate(expiryDate.getDate() + parseInt(days));
        
        const licenseData = {
            name: name.trim().toUpperCase(),
            expiry: expiryDate.toISOString(),
            generated: new Date().toISOString(),
            type: type,
            version: "4.0",
            deviceId: this.generateDeviceId()
        };
        
        const jsonString = JSON.stringify(licenseData);
        
        // Double encryption for security
        const encrypted1 = CryptoJS.AES.encrypt(jsonString, MASTER_KEY).toString();
        const encrypted2 = CryptoJS.AES.encrypt(encrypted1, BACKUP_KEY).toString();
        const base64 = btoa(encrypted2);
        
        return `CA-PRO-${type.toUpperCase()}-${base64}`;
    }
    
    static validateLicense(licenseKey) {
        try {
            // Validate format
            if (!licenseKey.startsWith('CA-PRO-')) {
                return { 
                    valid: false, 
                    error: 'Invalid format. Must start with CA-PRO-',
                    code: 'FORMAT_ERROR'
                };
            }
            
            // Extract parts
            const parts = licenseKey.split('-');
            if (parts.length < 4) {
                return { 
                    valid: false, 
                    error: 'Invalid license structure',
                    code: 'STRUCTURE_ERROR'
                };
            }
            
            const licenseType = parts[2];
            const base64 = parts.slice(3).join('-');
            
            // Decrypt
            const encrypted2 = atob(base64);
            const encrypted1 = CryptoJS.AES.decrypt(encrypted2, BACKUP_KEY);
            const intermediate = encrypted1.toString(CryptoJS.enc.Utf8);
            
            if (!intermediate) {
                return { 
                    valid: false, 
                    error: 'Invalid license key',
                    code: 'DECRYPT_ERROR'
                };
            }
            
            const decrypted = CryptoJS.AES.decrypt(intermediate, MASTER_KEY);
            const jsonString = decrypted.toString(CryptoJS.enc.Utf8);
            
            if (!jsonString) {
                return { 
                    valid: false, 
                    error: 'Invalid license data',
                    code: 'DATA_ERROR'
                };
            }
            
            const licenseData = JSON.parse(jsonString);
            
            // Check if license is for current device
            const currentDeviceId = this.getDeviceId();
            if (licenseData.deviceId && licenseData.deviceId !== currentDeviceId) {
                return { 
                    valid: false, 
                    error: 'License is registered to another device',
                    code: 'DEVICE_MISMATCH',
                    data: licenseData
                };
            }
            
            const expiryDate = new Date(licenseData.expiry);
            const currentDate = new Date();
            
            // Check expiry
            if (expiryDate < currentDate) {
                return { 
                    valid: false, 
                    error: 'License has expired',
                    code: 'EXPIRED',
                    data: licenseData,
                    expiry: expiryDate
                };
            }
            
            // Calculate days remaining
            const daysRemaining = Math.ceil((expiryDate - currentDate) / (1000 * 60 * 60 * 24));
            
            return {
                valid: true,
                data: licenseData,
                expiry: expiryDate,
                daysRemaining: daysRemaining,
                type: licenseType
            };
            
        } catch (error) {
            console.error('License validation error:', error);
            return { 
                valid: false, 
                error: 'Invalid license key format',
                code: 'VALIDATION_ERROR'
            };
        }
    }
    
    static generateDeviceId() {
        // Generate a unique device ID based on browser fingerprint
        const fingerprint = [
            navigator.userAgent,
            navigator.platform,
            screen.width,
            screen.height,
            screen.colorDepth,
            new Date().getTimezoneOffset()
        ].join('|');
        
        return CryptoJS.MD5(fingerprint).toString();
    }
    
    static getDeviceId() {
        let deviceId = localStorage.getItem('ca_pro_device_id');
        if (!deviceId) {
            deviceId = this.generateDeviceId();
            localStorage.setItem('ca_pro_device_id', deviceId);
        }
        return deviceId;
    }
    
    static getLicenseStatus() {
        const licenseData = DataStorage.load('license');
        if (!licenseData) return { status: 'inactive' };
        
        const validation = this.validateLicense(licenseData.key);
        
        if (validation.valid) {
            return {
                status: 'active',
                type: validation.type,
                expiry: validation.expiry,
                daysRemaining: validation.daysRemaining,
                name: validation.data.name
            };
        } else {
            return {
                status: 'expired',
                error: validation.error,
                expiry: validation.expiry
            };
        }
    }
}
        
        // ====================
        // DATA STORAGE SYSTEM
        // ====================
        class DataStorage {
            static save(key, data) {
                localStorage.setItem(`ca_pro_${key}`, JSON.stringify(data));
            }
            
            static load(key) {
                const data = localStorage.getItem(`ca_pro_${key}`);
                return data ? JSON.parse(data) : null;
            }
            
            static clear() {
                // Keep license and user info
                const license = this.load('license');
                const user = this.load('user');
                localStorage.clear();
                
                if (license) this.save('license', license);
                if (user) this.save('user', user);
            }
            
            static exportAll() {
                const allData = {
                    user: this.load('user'),
                    progress: this.load('progress'),
                    targets: this.load('targets'),
                    completedTargets: this.load('completedTargets'),
                    studyPlan: this.load('studyPlan'),
                    videos: this.load('videos'),
                    resources: this.load('resources'),
                    stats: this.load('stats'),
                    exportDate: new Date().toISOString(),
                    version: '4.0'
                };
                
                return JSON.stringify(allData, null, 2);
            }
            
            static importAll(jsonData) {
                try {
                    const data = JSON.parse(jsonData);
                    
                    if (!data.version) {
                        return { success: false, error: 'Invalid backup file format' };
                    }
                    
                    // Save all data
                    Object.keys(data).forEach(key => {
                        if (key !== 'exportDate' && key !== 'version') {
                            this.save(key, data[key]);
                        }
                    });
                    
                    return { success: true };
                } catch (error) {
                    return { success: false, error: 'Invalid backup file' };
                }
            }
        }
        
        // ====================
        // GLOBAL VARIABLES
        // ====================
        let currentUser = null;
        let userProgress = {};
        let userTargets = [];
        let completedTargets = [];
        let userStudyPlan = {};
        let userVideos = [];
        let userResources = [];
        let userStats = {};
        let progressChart = null;
        let currentChartType = 'subject';
        let currentSubjectFilter = 'all';
        let notificationTimer = null;
        
        // CA Syllabus Data
        const syllabusData = {
            paper1: {
                name: "Paper 1: Financial Reporting (FR)",
                weightage: 26,
                topics: [
                    {
                        name: "Introduction to GPFS",
                        weightage: 12.5,
                        priority: "B",
                        subtopics: [
                            { name: "Ind AS 1 basics", weight: 40 },
                            { name: "Schedule III Div II", weight: 20 },
                            { name: "Applicability criteria", weight: 20 }
                        ]
                    },
                    {
                        name: "Conceptual Framework",
                        weightage: 7.5,
                        priority: "A",
                        subtopics: [
                            { name: "Application to GPFS", weight: 100 }
                        ]
                    },
                    {
                        name: "Presentation of Items",
                        weightage: 7.5,
                        priority: "A",
                        subtopics: [
                            { name: "Ind AS 1: Presentation", weight: 37.5 },
                            { name: "Ind AS 34: Interim", weight: 25 },
                            { name: "Ind AS 7: Cash Flows", weight: 37.5 }
                        ]
                    },
                    {
                        name: "Measurement Policies",
                        weightage: 7.5,
                        priority: "A",
                        subtopics: [
                            { name: "Ind AS 8: Policies/Errors", weight: 42.9 },
                            { name: "Ind AS 10: Events", weight: 28.6 },
                            { name: "Ind AS 113: Fair Value", weight: 28.6 }
                        ]
                    },
                    {
                        name: "Income Statement",
                        weightage: 7.5,
                        priority: "A",
                        subtopics: [
                            { name: "Ind AS 115: Revenue", weight: 100 }
                        ]
                    },
                    {
                        name: "Assets",
                        weightage: 20,
                        priority: "A",
                        subtopics: [
                            { name: "Ind AS 2: Inventories", weight: 16.7 },
                            { name: "Ind AS 16: PPE", weight: 20.8 },
                            { name: "Ind AS 116: Leases", weight: 25 },
                            { name: "Ind AS 23: Borrowing Costs", weight: 8.3 },
                            { name: "Ind AS 36: Impairment", weight: 16.7 },
                            { name: "Ind AS 38: Intangibles", weight: 12.5 }
                        ]
                    },
                    {
                        name: "Liabilities",
                        weightage: 20,
                        priority: "A",
                        subtopics: [
                            { name: "Ind AS 19: Employee Benefits", weight: 50 },
                            { name: "Ind AS 37: Provisions", weight: 50 }
                        ]
                    },
                    {
                        name: "Items Impacting FS",
                        weightage: 17.5,
                        priority: "A",
                        subtopics: [
                            { name: "Ind AS 12: Income Taxes", weight: 80 },
                            { name: "Ind AS 21: FX Rates", weight: 20 }
                        ]
                    },
                    {
                        name: "Disclosures",
                        weightage: 17.5,
                        priority: "B",
                        subtopics: [
                            { name: "Ind AS 24: Related Parties", weight: 33.3 },
                            { name: "Ind AS 33: EPS", weight: 33.3 },
                            { name: "Ind AS 108: Segments", weight: 33.3 }
                        ]
                    },
                    {
                        name: "Financial Instruments",
                        weightage: 12.5,
                        priority: "A",
                        subtopics: [
                            { name: "Ind AS 32/107/109 Classification", weight: 50 },
                            { name: "Derivatives/Hedge Accounting", weight: 50 }
                        ]
                    },
                    {
                        name: "Group Accounting",
                        weightage: 15,
                        priority: "A",
                        subtopics: [
                            { name: "Ind AS 103: Business Combos", weight: 55.6 },
                            { name: "Ind AS 110/111/28: Consolidation", weight: 44.4 }
                        ]
                    },
                    {
                        name: "First-time Adoption",
                        weightage: 7.5,
                        priority: "B",
                        subtopics: [
                            { name: "Ind AS 101", weight: 100 }
                        ]
                    },
                    {
                        name: "FS Analysis",
                        weightage: 7.5,
                        priority: "B",
                        subtopics: [
                            { name: "Ind AS based analysis", weight: 100 }
                        ]
                    },
                    {
                        name: "Ethics & Tech",
                        weightage: 7.5,
                        priority: "C",
                        subtopics: [
                            { name: "Ethical issues", weight: 66.7 },
                            { name: "Technology evolution", weight: 33.3 }
                        ]
                    }
                ]
            },
            paper2: {
                name: "Paper 2: Advanced Financial Management (AFM)",
                weightage: 22,
                topics: [
                    {
                        name: "Financial Policy",
                        weightage: 11.5,
                        priority: "B",
                        subtopics: [
                            { name: "CFO role + Value creation", weight: 50 },
                            { name: "Strategic framework", weight: 50 }
                        ]
                    },
                    {
                        name: "Risk Management",
                        weightage: 11.5,
                        priority: "A",
                        subtopics: [
                            { name: "Risk types + VaR", weight: 100 }
                        ]
                    },
                    {
                        name: "Advanced Cap Budgeting",
                        weightage: 25,
                        priority: "A",
                        subtopics: [
                            { name: "Inflation/Tech impact", weight: 50 },
                            { name: "Risk methods + APV", weight: 50 }
                        ]
                    },
                    {
                        name: "Security Analysis",
                        weightage: 25,
                        priority: "A",
                        subtopics: [
                            { name: "Fundamental Analysis", weight: 50 },
                            { name: "Technical Analysis + EMH", weight: 50 }
                        ]
                    },
                    {
                        name: "Security Valuation",
                        weightage: 25,
                        priority: "A",
                        subtopics: [
                            { name: "Equity/Pref/Debentures", weight: 50 },
                            { name: "CAPM + Risk premium", weight: 50 }
                        ]
                    },
                    {
                        name: "Portfolio Management",
                        weightage: 25,
                        priority: "A",
                        subtopics: [
                            { name: "Analysis/Selection/Revision", weight: 66.7 },
                            { name: "Securitization + Mutual Funds", weight: 33.3 }
                        ]
                    },
                    {
                        name: "Derivatives",
                        weightage: 22.5,
                        priority: "A",
                        subtopics: [
                            { name: "Forwards/Futures/Options", weight: 50 },
                            { name: "Swaps + Greeks", weight: 50 }
                        ]
                    },
                    {
                        name: "Forex Risk",
                        weightage: 22.5,
                        priority: "A",
                        subtopics: [
                            { name: "FX factors + SWIFT", weight: 100 }
                        ]
                    },
                    {
                        name: "Business Valuation",
                        weightage: 12.5,
                        priority: "A",
                        subtopics: [
                            { name: "Asset/Earning/Cashflow models", weight: 50 },
                            { name: "CAPM/APT + EVA/MVA", weight: 50 }
                        ]
                    },
                    {
                        name: "Mergers",
                        weightage: 3.5,
                        priority: "A",
                        subtopics: [
                            { name: "M&A + Takeovers + LBO", weight: 100 }
                        ]
                    },
                    {
                        name: "Startup Finance",
                        weightage: 3.5,
                        priority: "B",
                        subtopics: [
                            { name: "Unicorns + Funding", weight: 100 }
                        ]
                    }
                ]
            },
            paper3: {
                name: "Paper 3: Auditing and Assurance (Audit)",
                weightage: 20,
                topics: [
                    {
                        name: "Quality Control",
                        weightage: 50,
                        priority: "A",
                        subtopics: [
                            { name: "SQC 1 + SA 220", weight: 100 }
                        ]
                    },
                    {
                        name: "General Principles",
                        weightage: 50,
                        priority: "A",
                        subtopics: [
                            { name: "SA 240 (Fraud) + SA 250", weight: 50 },
                            { name: "SA 260 + SA 299 (Joint Audit)", weight: 50 }
                        ]
                    },
                    {
                        name: "Audit Planning",
                        weightage: 50,
                        priority: "A",
                        subtopics: [
                            { name: "SA 300/450/520/540", weight: 100 }
                        ]
                    },
                    {
                        name: "Materiality & Risk",
                        weightage: 4.5,
                        priority: "A",
                        subtopics: [
                            { name: "SA 265/315/320/330", weight: 100 }
                        ]
                    },
                    {
                        name: "Audit Evidence",
                        weightage: 4.5,
                        priority: "A",
                        subtopics: [
                            { name: "SA 500/501/505/530", weight: 100 }
                        ]
                    },
                    {
                        name: "Completion",
                        weightage: 20.5,
                        priority: "A",
                        subtopics: [
                            { name: "SA 560/570/580", weight: 100 }
                        ]
                    },
                    {
                        name: "Reporting",
                        weightage: 20.5,
                        priority: "A",
                        subtopics: [
                            { name: "SA 700/701/705/706", weight: 100 }
                        ]
                    },
                    {
                        name: "Special Audits",
                        weightage: 20.5,
                        priority: "A",
                        subtopics: [
                            { name: "Banks + NBFC + PSU", weight: 100 }
                        ]
                    },
                    {
                        name: "Digital Auditing",
                        weightage: 4.5,
                        priority: "A",
                        subtopics: [
                            { name: "CAATs + AI + Data Analytics", weight: 100 }
                        ]
                    },
                    {
                        name: "Professional Ethics",
                        weightage: 20.5,
                        priority: "A",
                        subtopics: [
                            { name: "CA Act + IESBA Code", weight: 100 }
                        ]
                    }
                ]
            },
            paper4: {
                name: "Paper 4: Direct Tax (DT)",
                weightage: 16,
                topics: [
                    {
                        name: "Company Taxation",
                        weightage: 42.5,
                        priority: "A",
                        subtopics: [
                            { name: "Computation - General Provisions", weight: 40 },
                            { name: "Special Regimes (115BAA/BAB)", weight: 26.7 },
                            { name: "Charitable Trusts (Sec 11-13)", weight: 16.7 },
                            { name: "GAAR + Business Trusts", weight: 16.7 }
                        ]
                    },
                    {
                        name: "Tax Administration",
                        weightage: 25,
                        priority: "B",
                        subtopics: [
                            { name: "TDS/TCS (Sec 192-206C)", weight: 40 },
                            { name: "Assessment Procedures", weight: 33.3 },
                            { name: "Appeals + Revision", weight: 26.7 }
                        ]
                    },
                    {
                        name: "International Taxation",
                        weightage: 32.5,
                        priority: "A",
                        subtopics: [
                            { name: "Transfer Pricing (Sec 92)", weight: 40 },
                            { name: "Non-Resident Taxation", weight: 32 },
                            { name: "DTAA + BEPS", weight: 28 }
                        ]
                    }
                ]
            },
            paper5: {
                name: "Paper 5: Indirect Tax (IDT)",
                weightage: 12,
                topics: [
                    {
                        name: "GST - Core Concepts",
                        weightage: 55,
                        priority: "A",
                        subtopics: [
                            { name: "Levy/Collection CGST/IGST", weight: 27.8 },
                            { name: "Supply (Sec 7 + Sch I/II/III)", weight: 22.2 },
                            { name: "Charge of Tax + RCM", weight: 16.7 },
                            { name: "ITC (Sec 16-18)", weight: 33.3 }
                        ]
                    },
                    {
                        name: "GST - Procedures",
                        weightage: 20,
                        priority: "B",
                        subtopics: [
                            { name: "Registration (Sec 22-29)", weight: 50 },
                            { name: "Returns (GSTR 1/3B/9)", weight: 50 }
                        ]
                    },
                    {
                        name: "GST - Compliance",
                        weightage: 22.5,
                        priority: "B",
                        subtopics: [
                            { name: "Demands/Recovery (Sec 73-76)", weight: 57.1 },
                            { name: "Appeals/Advance Ruling", weight: 42.9 }
                        ]
                    },
                    {
                        name: "Customs",
                        weightage: 52.5,
                        priority: "B",
                        subtopics: [
                            { name: "Levy + Classification", weight: 57.1 },
                            { name: "Import/Export Procedures", weight: 42.9 }
                        ]
                    },
                    {
                        name: "FTP",
                        weightage: 15,
                        priority: "C",
                        subtopics: [
                            { name: "Export Promotion Schemes", weight: 100 }
                        ]
                    }
                ]
            },
            paper6: {
                name: "Paper 6: Integrated Business Solutions (IBS)",
                weightage: 4,
                topics: [
                    {
                        name: "Multi-disciplinary Cases",
                        weightage: 100,
                        priority: "A",
                        subtopics: [
                            { name: "FR + AFM + Audit + DT + IDT", weight: 100 }
                        ]
                    }
                ]
            }
        };
        
        // Calculate total topics
        let totalTopics = 0;
        for (const subjectId in syllabusData) {
            totalTopics += syllabusData[subjectId].topics.length;
        }
        
        // ====================
        // DOM ELEMENTS
        // ====================
        const licenseContainer = document.getElementById('license-container');
        const appContainer = document.getElementById('app-container');
        const activateBtn = document.getElementById('activate-btn');
        const licenseKeyInput = document.getElementById('license-key');
        const licenseMessage = document.getElementById('license-message');
        const navLinks = document.querySelectorAll('.nav-link');
        const pages = document.querySelectorAll('.page');
        const userAvatar = document.getElementById('user-avatar');
        const userName = document.getElementById('user-name');
        const exportBtn = document.getElementById('export-btn');
        const importBtn = document.getElementById('import-btn');
        const deleteEntryBtn = document.getElementById('delete-entry-btn');
        const settingsBtn = document.getElementById('settings-btn');
        const resetAppBtn = document.getElementById('reset-app-btn');
        const saveAllProgressBtn = document.getElementById('save-all-progress-btn');
        const subjectSelect = document.getElementById('subject-select');
        const subjectContent = document.getElementById('subject-content');
        const progressFilters = document.querySelectorAll('.progress-filter');
        const targetToggle = document.getElementById('target-toggle');
        const targetContent = document.getElementById('target-content');
        const calculatePlanBtn = document.getElementById('calculate-plan-btn');
        const savePlanBtn = document.getElementById('save-plan-btn');
        const resetPlanBtn = document.getElementById('reset-plan-btn');
        const updateTargetBtn = document.getElementById('update-target-btn');
        const planningToggle = document.getElementById('planning-toggle');
        const planningForm = document.getElementById('planning-form');
        const chartTypeBtns = document.querySelectorAll('.chart-type-btn');
        const backupAllBtn = document.getElementById('backup-all-btn');
        const restoreBackupBtn = document.getElementById('restore-backup-btn');
        const backupFile = document.getElementById('backup-file');
        
        // ====================
        // INITIALIZATION
        // ====================
        document.addEventListener('DOMContentLoaded', function() {
            // Set today's date in inputs
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('start-date').value = today;
            document.getElementById('target-date').value = today;
            
            // Check for valid license
            checkLicense();
        });
        
        function checkLicense() {
            const licenseData = DataStorage.load('license');
            
            if (!licenseData) {
                // Show license screen
                licenseContainer.style.display = 'flex';
                appContainer.style.display = 'none';
                return;
            }
            
            const validation = LicenseSystem.validateLicense(licenseData.key);
            
            if (!validation.valid) {
                // Invalid or expired license
                licenseContainer.style.display = 'flex';
                appContainer.style.display = 'none';
                licenseKeyInput.value = licenseData.key;
                showLicenseMessage('License expired or invalid. Please renew.', 'error');
                return;
            }
            
            // Valid license
            currentUser = validation.data;
            licenseContainer.style.display = 'none';
            appContainer.style.display = 'block';
            
            // Initialize app
            setupEventListeners();
            loadUserData();
            setupNotifications();
        }
        
        function setupEventListeners() {
            // License activation
            activateBtn.addEventListener('click', activateLicense);
            licenseKeyInput.addEventListener('keypress', (e) => {
                if (e.key === 'Enter') activateLicense();
            });
            
            // Navigation
            navLinks.forEach(link => {
                link.addEventListener('click', (e) => {
                    e.preventDefault();
                    const pageId = link.getAttribute('data-page');
                    
                    navLinks.forEach(l => l.classList.remove('active'));
                    link.classList.add('active');
                    
                    pages.forEach(page => page.classList.remove('active'));
                    document.getElementById(`${pageId}-page`).classList.add('active');
                    
                    if (pageId === 'subjects') {
                        loadSubjectContent();
                    } else if (pageId === 'dashboard') {
                        updateDashboard();
                    } else if (pageId === 'planning') {
                        loadPlanningPage();
                    } else if (pageId === 'videos') {
                        loadVideos();
                    } else if (pageId === 'resources') {
                        loadResources();
                    }
                });
            });
            
            // User menu
            document.getElementById('user-menu-btn').addEventListener('click', () => {
                document.getElementById('user-dropdown').classList.toggle('active');
            });
            
            // Close dropdown when clicking outside
            document.addEventListener('click', (e) => {
                if (!e.target.closest('.user-menu')) {
                    document.getElementById('user-dropdown').classList.remove('active');
                }
            });
            
            // Data management
            exportBtn.addEventListener('click', exportUserData);
            importBtn.addEventListener('click', () => {
                document.getElementById('import-modal').classList.add('active');
            });
            
            deleteEntryBtn.addEventListener('click', () => {
                document.getElementById('delete-entry-modal').classList.add('active');
                loadDeleteEntries('targets');
            });
            
            resetAppBtn.addEventListener('click', resetApp);
            
            // Study plan
            targetToggle.addEventListener('click', () => {
                targetContent.classList.toggle('expanded');
                const icon = targetToggle.querySelector('i');
                icon.classList.toggle('fa-chevron-down');
                icon.classList.toggle('fa-chevron-up');
            });
            
            updateTargetBtn.addEventListener('click', () => {
                targetContent.classList.add('expanded');
                const icon = targetToggle.querySelector('i');
                icon.classList.remove('fa-chevron-down');
                icon.classList.add('fa-chevron-up');
            });
            
            calculatePlanBtn.addEventListener('click', calculateStudyPlan);
            savePlanBtn.addEventListener('click', saveStudyPlan);
            resetPlanBtn.addEventListener('click', resetStudyPlan);
            
            // Progress filters - Updated to filter KPIs
            progressFilters.forEach(filter => {
                filter.addEventListener('click', () => {
                    progressFilters.forEach(f => f.classList.remove('active'));
                    filter.classList.add('active');
                    currentSubjectFilter = filter.getAttribute('data-filter');
                    updateDashboard();
                    updateSubjectFilteredKPIs();
                });
            });
            
            // Chart type buttons
            chartTypeBtns.forEach(btn => {
                btn.addEventListener('click', () => {
                    chartTypeBtns.forEach(b => b.classList.remove('active'));
                    btn.classList.add('active');
                    currentChartType = btn.getAttribute('data-chart');
                    updateChart();
                });
            });
            
            // Subjects
            subjectSelect.addEventListener('change', loadSubjectContent);
            saveAllProgressBtn.addEventListener('click', saveAllProgress);
            
            // Planning
            planningToggle.addEventListener('click', () => {
                planningToggle.classList.toggle('expanded');
                planningForm.classList.toggle('expanded');
            });
            
            document.getElementById('planning-subject').addEventListener('change', loadTopicsForPlanning);
            document.getElementById('planning-topic').addEventListener('change', loadSubtopicsForPlanning);
            document.getElementById('save-target-btn').addEventListener('click', saveTarget);
            document.getElementById('target-status-filter').addEventListener('change', loadTargets);
            document.getElementById('target-subject-filter').addEventListener('change', loadTargets);
            document.getElementById('search-history').addEventListener('input', loadTargetHistory);
            
            // Videos
            document.getElementById('video-toggle').addEventListener('click', () => {
                document.getElementById('video-toggle').classList.toggle('expanded');
                document.getElementById('video-form').classList.toggle('expanded');
            });
            document.getElementById('add-video-btn').addEventListener('click', addVideo);
            document.getElementById('search-videos').addEventListener('input', searchVideos);
            
            // Resources
            document.getElementById('resources-toggle').addEventListener('click', () => {
                document.getElementById('resources-toggle').classList.toggle('expanded');
                document.getElementById('resources-form').classList.toggle('expanded');
            });
            document.getElementById('add-resources-btn').addEventListener('click', addResource);
            document.getElementById('search-resources').addEventListener('input', searchResources);
            
            // Data backup
            backupAllBtn.addEventListener('click', backupAllData);
            restoreBackupBtn.addEventListener('click', () => backupFile.click());
            backupFile.addEventListener('change', restoreBackupData);
            
            // Modals
            document.getElementById('import-close').addEventListener('click', () => {
                document.getElementById('import-modal').classList.remove('active');
            });
            
            document.getElementById('delete-entry-close').addEventListener('click', () => {
                document.getElementById('delete-entry-modal').classList.remove('active');
            });
            
            document.getElementById('confirm-delete-btn').addEventListener('click', deleteSelectedEntries);
            
            // Entry type selector
            document.querySelectorAll('.entry-type-btn').forEach(btn => {
                btn.addEventListener('click', function() {
                    document.querySelectorAll('.entry-type-btn').forEach(b => b.classList.remove('active'));
                    this.classList.add('active');
                    const type = this.getAttribute('data-type');
                    loadDeleteEntries(type);
                });
            });
            
            // Process backup
            document.getElementById('process-backup-btn').addEventListener('click', processBackup);
            
            // Close modals on outside click
            document.querySelectorAll('.modal').forEach(modal => {
                modal.addEventListener('click', (e) => {
                    if (e.target === modal) {
                        modal.classList.remove('active');
                    }
                });
            });
            
            // Home button
            document.getElementById('home-btn').addEventListener('click', () => {
                navLinks.forEach(l => l.classList.remove('active'));
                document.querySelector('.nav-link[data-page="dashboard"]').classList.add('active');
                
                pages.forEach(page => page.classList.remove('active'));
                document.getElementById('dashboard-page').classList.add('active');
                
                updateDashboard();
            });
        }
        
        // ====================
        // SUBJECT-FILTERED KPI CALCULATIONS
        // ====================
        function updateSubjectFilteredKPIs() {
            const filter = currentSubjectFilter;
            
            if (filter === 'all') {
                calculateKPIs();
                return;
            }
            
            // Calculate subject-specific KPIs
            let subjectActualHours = 0;
            let subjectPlannedHours = userStudyPlan?.subjectHours?.[filter] || 0;
            let completedTopics = 0;
            let totalSubjectTopics = syllabusData[filter]?.topics.length || 0;
            
            const subjectProgress = userProgress?.subjects?.[filter];
            if (subjectProgress && subjectProgress.topics) {
                Object.values(subjectProgress.topics).forEach(topic => {
                    if (topic.status === 'completed') completedTopics++;
                    
                    if (topic.subtopics) {
                        Object.values(topic.subtopics).forEach(subtopic => {
                            subjectActualHours += subtopic.actualHours || 0;
                        });
                    }
                });
            }
            
            let subjectTargets = [...userTargets, ...completedTargets]
                .filter(t => t.subject === filter);
            let subjectAchievedTargets = subjectTargets.filter(t => t.completed).length;
            
            // Update KPI cards
            document.getElementById('actual-hours').textContent = Math.round(subjectActualHours * 10) / 10;
            document.getElementById('planned-hours').textContent = subjectPlannedHours;
            document.getElementById('overall-progress').textContent = subjectPlannedHours > 0 ? 
                `${Math.round((subjectActualHours / subjectPlannedHours) * 100)}%` : "0%";
            document.getElementById('subjects-completed').textContent = "N/A";
            document.getElementById('topics-completed').textContent = completedTopics;
            document.getElementById('total-topics').textContent = totalSubjectTopics;
            document.getElementById('plan-completion').textContent = subjectPlannedHours > 0 ? 
                `${Math.round((subjectActualHours / subjectPlannedHours) * 100)}%` : "0%";
            document.getElementById('targets-achieved').textContent = `${subjectAchievedTargets}/${subjectTargets.length}`;
            
            if (subjectTargets.length > 0) {
                const successRate = Math.round((subjectAchievedTargets / subjectTargets.length) * 100);
                document.getElementById('success-rate').textContent = `${successRate}%`;
            }
        }
        
        // ====================
        // IMPROVED STUDY PLAN CALCULATIONS
        // ====================
        function calculateStudyPlan() {
            const daysToExam = parseInt(document.getElementById('days-to-exam').value) || 180;
            const dailyHours = parseFloat(document.getElementById('daily-hours').value) || 4;
            const startDate = document.getElementById('start-date').value || new Date().toISOString().split('T')[0];
            
            const totalHours = Math.round(daysToExam * dailyHours);
            
            const start = new Date(startDate);
            const examDate = new Date(start);
            examDate.setDate(start.getDate() + daysToExam);
            
            document.getElementById('total-days').textContent = daysToExam;
            document.getElementById('total-hours').textContent = totalHours;
            document.getElementById('daily-target').textContent = dailyHours;
            document.getElementById('exam-date').textContent = examDate.toLocaleDateString('en-US', { 
                year: 'numeric', 
                month: 'short', 
                day: 'numeric' 
            });
            
            window.tempStudyPlan = {
                daysToExam: daysToExam,
                dailyHours: dailyHours,
                totalHours: totalHours,
                startDate: startDate,
                examDate: examDate.toISOString().split('T')[0],
                subjectHours: {}
            };
            
            const subjectHoursList = document.getElementById('subject-hours-list');
            subjectHoursList.innerHTML = '';
            
            let totalAllocated = 0;
            for (const subjectId in syllabusData) {
                const subjectWeight = syllabusData[subjectId].weightage;
                const calculatedHours = Math.round((subjectWeight / 100) * totalHours);
                totalAllocated += calculatedHours;
                
                const subjectRow = document.createElement('div');
                subjectRow.className = 'subject-hour-item';
                subjectRow.innerHTML = `
                    <span>${syllabusData[subjectId].name}</span>
                    <div>
                        <input type="number" 
                               class="hour-input" 
                               data-subject="${subjectId}" 
                               value="${calculatedHours}" 
                               min="0" 
                               max="${totalHours}"
                               onchange="updateHourDistribution()">
                        <span>hours</span>
                    </div>
                `;
                subjectHoursList.appendChild(subjectRow);
            }
            
            document.querySelectorAll('.hour-input').forEach(input => {
                input.addEventListener('change', updateHourDistribution);
                input.addEventListener('input', updateHourDistribution);
            });
            
            document.getElementById('subject-hours-distribution').style.display = 'block';
            document.getElementById('target-display').style.display = 'block';
            
            updateHourDistribution();
            
            document.getElementById('target-display').scrollIntoView({ behavior: 'smooth' });
        }
        
        function updateHourDistribution() {
            const totalHours = window.tempStudyPlan?.totalHours || 720;
            let totalAllocated = 0;
            
            document.querySelectorAll('.hour-input').forEach(input => {
                totalAllocated += parseInt(input.value) || 0;
            });
            
            const remaining = totalHours - totalAllocated;
            const hoursSummary = document.getElementById('hours-summary');
            
            hoursSummary.innerHTML = `Total Allocated: ${totalAllocated} hours | Remaining: ${remaining} hours`;
            hoursSummary.className = remaining >= 0 ? 'hours-summary' : 'hours-summary negative';
            
            if (remaining < 0) {
                hoursSummary.innerHTML += '<br><small style="font-weight: normal;">Reduce some hours to match total</small>';
            }
        }
        
        // ====================
        // SMART PACE CALCULATIONS
        // ====================
        function calculateSmartPace() {
            const totalPlannedHours = userStudyPlan?.totalHours || 720;
            const totalActualHours = userStats.totalHoursStudied || 0;
            const daysStudied = userStats.totalStudyDays || 1;
            
            const dailyVelocity = daysStudied > 0 ? totalActualHours / daysStudied : 0;
            const remainingHours = Math.max(0, totalPlannedHours - totalActualHours);
            const daysRemainingByVelocity = dailyVelocity > 0 ? Math.ceil(remainingHours / dailyVelocity) : 999;
            const daysRemainingFromPlan = userStudyPlan?.daysToExam - daysStudied;
            const bufferDays = daysRemainingFromPlan - daysRemainingByVelocity;
            
            document.getElementById('current-pace').textContent = `${Math.round(dailyVelocity * 10) / 10} hrs/day`;
            
            const paceStatus = document.getElementById('pace-status');
            if (bufferDays > 7) {
                paceStatus.innerHTML = '<span class="performance-good">Ahead by ' + bufferDays + ' days!</span>';
            } else if (bufferDays > 0) {
                paceStatus.innerHTML = '<span class="performance-good">Slightly ahead</span>';
            } else if (bufferDays === 0) {
                paceStatus.innerHTML = '<span class="performance-good">Exactly on track</span>';
            } else if (bufferDays >= -7) {
                paceStatus.innerHTML = '<span class="performance-neutral">Slightly behind</span>';
            } else {
                paceStatus.innerHTML = '<span class="performance-bad">Behind by ' + Math.abs(bufferDays) + ' days</span>';
            }
            
            const projectionText = document.getElementById('projection-text');
            if (remainingHours <= 0) {
                projectionText.innerHTML = `🎉 <strong>Congratulations!</strong> You've completed your study plan!`;
            } else if (bufferDays > 0) {
                projectionText.innerHTML = `📈 <strong>Great progress!</strong> At current pace (${Math.round(dailyVelocity * 10) / 10} hrs/day), you'll finish in <strong>${daysRemainingByVelocity} days</strong>. You're <strong>${bufferDays} days ahead</strong> of schedule!`;
            } else if (bufferDays < 0) {
                projectionText.innerHTML = `📊 <strong>Current pace:</strong> ${Math.round(dailyVelocity * 10) / 10} hrs/day. You'll finish in <strong>${daysRemainingByVelocity} days</strong> (${Math.abs(bufferDays)} days behind). Consider increasing to <strong>${Math.round((remainingHours / daysRemainingFromPlan) * 10) / 10} hrs/day</strong> to catch up.`;
            } else {
                projectionText.innerHTML = `📊 <strong>On track!</strong> Current pace (${Math.round(dailyVelocity * 10) / 10} hrs/day) matches your plan. Keep it up!`;
            }
        }
        
        // ====================
        // INSTANT TARGET COMPLETION
        // ====================
        function completeTarget(index) {
            if (index >= 0 && index < userTargets.length) {
                const target = userTargets[index];
                target.completed = true;
                target.completedDate = new Date().toISOString();
                
                const achievement = target.plannedHours > 0 ? 
                    Math.round((target.actualHours || 0) / target.plannedHours * 100) : 0;
                
                if (achievement >= 100) {
                    target.status = 'completed';
                } else if (achievement >= 80) {
                    target.status = 'almost-completed';
                } else {
                    target.status = 'partial';
                }
                
                completedTargets.push(target);
                userTargets.splice(index, 1);
                
                DataStorage.save('targets', userTargets);
                DataStorage.save('completedTargets', completedTargets);
                
                loadTargets();
                loadTargetHistory();
                updateDashboard();
                
                showToast('Target moved to history!', 'success');
            }
        }
        
        // ====================
        // DELETE ENTRY FEATURE
        // ====================
        function loadDeleteEntries(type) {
            const entryListContainer = document.getElementById('entry-list-container');
            entryListContainer.innerHTML = '';
            
            let entries = [];
            let entryName = '';
            
            switch(type) {
                case 'targets':
                    entries = [...userTargets, ...completedTargets];
                    entryName = 'targets';
                    break;
                case 'videos':
                    entries = userVideos;
                    entryName = 'videos';
                    break;
                case 'resources':
                    entries = userResources;
                    entryName = 'resources';
                    break;
            }
            
            if (entries.length === 0) {
                entryListContainer.innerHTML = '<p style="color: var(--gray); text-align: center;">No entries found</p>';
                return;
            }
            
            entries.forEach((entry, index) => {
                const checkbox = document.createElement('div');
                checkbox.className = 'checkbox-item';
                
                let label = '';
                if (type === 'targets') {
                    label = `${entry.topic} (${getSubjectName(entry.subject)}) - ${formatDate(entry.date)}`;
                } else if (type === 'videos') {
                    label = `${entry.title} (${getSubjectName(entry.subject)})`;
                } else if (type === 'resources') {
                    label = `${entry.title} (${getSubjectName(entry.subject)} - ${entry.type})`;
                }
                
                checkbox.innerHTML = `
                    <input type="checkbox" id="entry-${type}-${index}" value="${entry.id || index}" data-type="${type}">
                    <label for="entry-${type}-${index}">${label}</label>
                `;
                entryListContainer.appendChild(checkbox);
            });
        }
        
        function deleteSelectedEntries() {
            const password = document.getElementById('delete-password').value;
            if (password !== 'R@dha') {
                document.getElementById('delete-message').textContent = 'Incorrect password!';
                document.getElementById('delete-message').className = 'license-message error';
                document.getElementById('delete-message').style.display = 'block';
                return;
            }
            
            const checkboxes = document.querySelectorAll('#entry-list-container input[type="checkbox"]:checked');
            if (checkboxes.length === 0) {
                document.getElementById('delete-message').textContent = 'Please select entries to delete.';
                document.getElementById('delete-message').className = 'license-message error';
                document.getElementById('delete-message').style.display = 'block';
                return;
            }
            
            if (!confirm(`Are you sure you want to delete ${checkboxes.length} selected entries?`)) {
                return;
            }
            
            const type = document.querySelector('.entry-type-btn.active').getAttribute('data-type');
            const idsToDelete = Array.from(checkboxes).map(cb => parseInt(cb.value));
            
            switch(type) {
                case 'targets':
                    userTargets = userTargets.filter((t, idx) => !idsToDelete.includes(idx));
                    completedTargets = completedTargets.filter((t, idx) => !idsToDelete.includes(idx + userTargets.length));
                    DataStorage.save('targets', userTargets);
                    DataStorage.save('completedTargets', completedTargets);
                    break;
                case 'videos':
                    userVideos = userVideos.filter(v => !idsToDelete.includes(v.id));
                    DataStorage.save('videos', userVideos);
                    break;
                case 'resources':
                    userResources = userResources.filter(r => !idsToDelete.includes(r.id));
                    DataStorage.save('resources', userResources);
                    break;
            }
            
            document.getElementById('delete-password').value = '';
            
            document.getElementById('delete-message').textContent = `${checkboxes.length} entries deleted successfully!`;
            document.getElementById('delete-message').className = 'license-message success';
            document.getElementById('delete-message').style.display = 'block';
            
            loadDeleteEntries(type);
            
            if (type === 'targets') {
                loadTargets();
                loadTargetHistory();
                updateDashboard();
            } else if (type === 'videos') {
                loadVideos();
            } else if (type === 'resources') {
                loadResources();
            }
            
            setTimeout(() => {
                document.getElementById('delete-message').style.display = 'none';
            }, 3000);
        }
        
        // ====================
        // SMART NOTIFICATIONS
        // ====================
        function setupNotifications() {
            if ('Notification' in window && Notification.permission === 'default') {
                Notification.requestPermission();
            }
            
            if (notificationTimer) {
                clearInterval(notificationTimer);
            }
            
            scheduleDailyNotification(6, 0, 'morning');
            scheduleDailyNotification(20, 0, 'evening');
            
            schedulePeriodicReminders();
        }
        
        function scheduleDailyNotification(hour, minute, timeOfDay) {
            const now = new Date();
            const notificationTime = new Date();
            notificationTime.setHours(hour, minute, 0, 0);
            
            if (now > notificationTime) {
                notificationTime.setDate(notificationTime.getDate() + 1);
            }
            
            const timeUntilNotification = notificationTime.getTime() - now.getTime();
            
            setTimeout(() => {
                sendSmartNotification(timeOfDay);
                scheduleDailyNotification(hour, minute, timeOfDay);
            }, timeUntilNotification);
        }
        
        function schedulePeriodicReminders() {
            notificationTimer = setInterval(() => {
                const now = new Date();
                const currentHour = now.getHours();
                
                if (currentHour >= 8 && currentHour <= 22) {
                    sendStudyReminder();
                }
            }, 4 * 60 * 60 * 1000);
        }
        
        function sendSmartNotification(timeOfDay) {
            if (Notification.permission !== 'granted') return;
            
            const today = new Date().toISOString().split('T')[0];
            const todayTargets = [...userTargets, ...completedTargets].filter(t => t.date === today);
            const totalPlannedToday = todayTargets.reduce((sum, t) => sum + (t.plannedHours || 0), 0);
            const totalActualToday = todayTargets.reduce((sum, t) => sum + (t.actualHours || 0), 0);
            
            let title = '';
            let body = '';
            
            if (timeOfDay === 'morning') {
                title = '🌅 CA Final Study Plan';
                if (totalPlannedToday > 0) {
                    body = `Good morning! You have ${totalPlannedToday} hours planned for today. Start strong!`;
                } else {
                    body = 'Good morning! Time to plan your study day. Set some targets!';
                }
            } else if (timeOfDay === 'evening') {
                title = '🌙 CA Final Progress Update';
                if (totalActualToday > 0) {
                    const efficiency = totalPlannedToday > 0 ? 
                        Math.round((totalActualToday / totalPlannedToday) * 100) : 100;
                    body = `You studied ${totalActualToday} hours today (${efficiency}% efficiency). Great job!`;
                } else {
                    body = 'Evening check-in: Have you studied today? Update your progress!';
                }
            }
            
            const remainingHours = userStudyPlan?.totalHours ? 
                Math.max(0, userStudyPlan.totalHours - (userStats.totalHoursStudied || 0)) : 0;
            const dailyVelocity = userStats.dailyVelocity || 0;
            
            if (remainingHours > 0 && dailyVelocity > 0) {
                const daysRemaining = Math.ceil(remainingHours / dailyVelocity);
                body += ` At this pace, you'll finish in ${daysRemaining} days.`;
            }
            
            new Notification(title, {
                body: body,
                icon: 'https://cdn-icons-png.flaticon.com/512/3135/3135715.png'
            });
        }
        
        function sendStudyReminder() {
            if (Notification.permission !== 'granted') return;
            
            const now = new Date();
            const currentHour = now.getHours();
            
            let message = '';
            if (currentHour < 12) {
                message = 'Morning study session? Update your progress!';
            } else if (currentHour < 17) {
                message = 'Afternoon study break? Log your hours!';
            } else {
                message = 'Evening study time? Track your progress!';
            }
            
            new Notification('📚 CA Study Reminder', {
                body: message,
                icon: 'https://cdn-icons-png.flaticon.com/512/3135/3135715.png'
            });
        }
        
        // ====================
        // GREEN CHART COLORS
        // ====================
        function updateChart() {
            const ctx = document.getElementById('progress-chart').getContext('2d');
            
            if (progressChart) {
                progressChart.destroy();
            }
            
            let chartData, chartOptions;
            
            if (currentChartType === 'subject') {
                let subjectsToShow = [];
                
                if (currentSubjectFilter === 'all') {
                    subjectsToShow = Object.keys(syllabusData);
                } else {
                    subjectsToShow = [currentSubjectFilter];
                }
                
                const subjectNames = subjectsToShow.map(subjectId => {
                    const name = syllabusData[subjectId].name;
                    return name.length > 20 ? name.substring(0, 20) + '...' : name;
                });
                
                const plannedHours = subjectsToShow.map(subjectId => {
                    return userStudyPlan?.subjectHours?.[subjectId] || 0;
                });
                
                const actualHours = subjectsToShow.map(subjectId => {
                    let subjectActual = 0;
                    if (userProgress.subjects[subjectId]) {
                        for (const topicName in userProgress.subjects[subjectId].topics) {
                            const topic = userProgress.subjects[subjectId].topics[topicName];
                            if (topic.subtopics) {
                                Object.values(topic.subtopics).forEach(subtopic => {
                                    subjectActual += subtopic.actualHours || 0;
                                });
                            }
                        }
                    }
                    return subjectActual;
                });
                
                const backgroundColors = actualHours.map((actual, index) => {
                    const planned = plannedHours[index];
                    const achievement = planned > 0 ? actual / planned : 0;
                    
                    if (achievement >= 1) return 'rgba(46, 204, 113, 0.9)';
                    if (achievement >= 0.8) return 'rgba(52, 211, 153, 0.8)';
                    if (achievement >= 0.6) return 'rgba(85, 239, 196, 0.7)';
                    if (achievement >= 0.4) return 'rgba(129, 236, 236, 0.6)';
                    if (achievement >= 0.2) return 'rgba(0, 184, 148, 0.5)';
                    return 'rgba(0, 150, 136, 0.4)';
                });
                
                chartData = {
                    labels: subjectNames,
                    datasets: [{
                        label: 'Actual Hours',
                        data: actualHours,
                        backgroundColor: backgroundColors,
                        borderColor: backgroundColors.map(color => color.replace('0.', '1.').slice(0, -1) + '1)'),
                        borderWidth: 2,
                        borderRadius: 8
                    }]
                };
                
                chartOptions = {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'top',
                        },
                        tooltip: {
                            callbacks: {
                                label: function(context) {
                                    let label = context.dataset.label || '';
                                    if (label) {
                                        label += ': ';
                                    }
                                    label += context.raw + ' hours';
                                    
                                    const planned = plannedHours[context.dataIndex];
                                    const actual = actualHours[context.dataIndex];
                                    if (planned > 0) {
                                        const percentage = Math.round((actual / planned) * 100);
                                        label += ` (${percentage}% of ${planned} planned)`;
                                    }
                                    return label;
                                }
                            }
                        }
                    },
                    scales: {
                        x: {
                            grid: {
                                display: false
                            }
                        },
                        y: {
                            beginAtZero: true,
                            title: {
                                display: true,
                                text: 'Hours'
                            },
                            grid: {
                                color: 'rgba(0, 0, 0, 0.05)'
                            }
                        }
                    }
                };
                
                progressChart = new Chart(ctx, {
                    type: 'bar',
                    data: chartData,
                    options: chartOptions
                });
                
            } else if (currentChartType === 'time') {
                const days = 7;
                const labels = [];
                const actualData = [];
                
                const today = new Date();
                for (let i = days - 1; i >= 0; i--) {
                    const date = new Date();
                    date.setDate(today.getDate() - i);
                    const dateStr = date.toISOString().split('T')[0];
                    
                    labels.push(date.toLocaleDateString('en-US', { weekday: 'short' }));
                    
                    const dayTargets = [...userTargets, ...completedTargets].filter(t => t.date === dateStr);
                    const actual = dayTargets.reduce((sum, t) => sum + (t.actualHours || 0), 0);
                    actualData.push(actual);
                }
                
                const greenGradient = ctx.createLinearGradient(0, 0, 0, 400);
                greenGradient.addColorStop(0, 'rgba(46, 204, 113, 0.8)');
                greenGradient.addColorStop(1, 'rgba(46, 204, 113, 0.2)');
                
                chartData = {
                    labels: labels,
                    datasets: [{
                        label: 'Actual Hours',
                        data: actualData,
                        borderColor: 'rgba(46, 204, 113, 1)',
                        backgroundColor: greenGradient,
                        borderWidth: 3,
                        fill: true,
                        tension: 0.4
                    }]
                };
                
                chartOptions = {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'top',
                        }
                    },
                    scales: {
                        y: {
                            beginAtZero: true,
                            title: {
                                display: true,
                                text: 'Hours'
                            }
                        }
                    }
                };
                
                progressChart = new Chart(ctx, {
                    type: 'line',
                    data: chartData,
                    options: chartOptions
                });
                
            } else if (currentChartType === 'targets') {
                const totalTargets = userStats.totalTargetsSet || 0;
                const achievedTargets = userStats.totalTargetsAchieved || 0;
                const pendingTargets = totalTargets - achievedTargets;
                
                let labels, data, backgroundColors;
                
                if (totalTargets === 0) {
                    labels = ['No Targets'];
                    data = [100];
                    backgroundColors = ['rgba(149, 165, 166, 0.7)'];
                } else {
                    labels = ['Achieved', 'Pending'];
                    data = [achievedTargets, pendingTargets];
                    backgroundColors = [
                        'rgba(46, 204, 113, 0.8)',
                        'rgba(0, 184, 148, 0.5)'
                    ];
                }
                
                chartData = {
                    labels: labels,
                    datasets: [{
                        data: data,
                        backgroundColor: backgroundColors,
                        borderColor: backgroundColors.map(color => color.replace('0.', '1.').slice(0, -1) + '1)'),
                        borderWidth: 2
                    }]
                };
                
                chartOptions = {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'top',
                        },
                        tooltip: {
                            callbacks: {
                                label: function(context) {
                                    const label = context.label || '';
                                    const value = context.raw || 0;
                                    const total = context.dataset.data.reduce((a, b) => a + b, 0);
                                    const percentage = Math.round((value / total) * 100);
                                    return `${label}: ${value} (${percentage}%)`;
                                }
                            }
                        }
                    }
                };
                
                progressChart = new Chart(ctx, {
                    type: 'doughnut',
                    data: chartData,
                    options: chartOptions
                });
            }
        }
        
        // ====================
        // HELPER FUNCTIONS
        // ====================
        function getSubjectName(subjectId) {
            const subjectMap = {
                'paper1': 'Paper 1: FR',
                'paper2': 'Paper 2: AFM',
                'paper3': 'Paper 3: Audit',
                'paper4': 'Paper 4: DT',
                'paper5': 'Paper 5: IDT',
                'paper6': 'Paper 6: IBS',
                'general': 'General'
            };
            return subjectMap[subjectId] || subjectId;
        }
        
        function formatDate(dateString) {
            const date = new Date(dateString);
            return date.toLocaleDateString('en-US', { 
                year: 'numeric', 
                month: 'short', 
                day: 'numeric' 
            });
        }
        
        function showToast(message, type = 'success') {
            const toast = document.getElementById('toast');
            const toastTitle = document.getElementById('toast-title');
            const toastMessage = document.getElementById('toast-message');
            const toastIcon = toast.querySelector('i');
            
            toastTitle.textContent = type === 'success' ? 'Success!' : 'Error!';
            toastMessage.textContent = message;
            
            toast.className = `toast ${type}`;
            toastIcon.className = type === 'success' ? 'fas fa-check-circle' : 'fas fa-exclamation-circle';
            
            toast.classList.add('show');
            
            setTimeout(() => {
                toast.classList.remove('show');
            }, 3000);
        }
        
        // ====================
        // UPDATED KPI CALCULATION
        // ====================
        function calculateKPIs() {
            let totalActualHours = 0;
            let totalPlannedHours = userStudyPlan?.totalHours || 720;
            let completedTopics = 0;
            let completedSubjects = 0;
            let totalSubtopicsCompleted = 0;
            let totalSubtopics = 0;
            
            let subjectsToCalculate = [];
            
            if (currentSubjectFilter === 'all') {
                subjectsToCalculate = Object.keys(syllabusData);
            } else {
                subjectsToCalculate = [currentSubjectFilter];
            }
            
            subjectsToCalculate.forEach(subjectId => {
                const subject = syllabusData[subjectId];
                const subjectProgress = userProgress?.subjects?.[subjectId] || { topics: {} };
                
                let subjectCompleted = true;
                
                if (subjectProgress.topics) {
                    for (const topicName in subjectProgress.topics) {
                        const topic = subjectProgress.topics[topicName];
                        
                        if (topic.status === 'completed') {
                            completedTopics++;
                        } else {
                            subjectCompleted = false;
                        }
                        
                        if (topic.subtopics) {
                            Object.values(topic.subtopics).forEach(subtopic => {
                                totalActualHours += subtopic.actualHours || 0;
                                totalSubtopics++;
                                if (subtopic.completed) {
                                    totalSubtopicsCompleted++;
                                }
                            });
                        }
                    }
                }
                
                if (subjectCompleted && subjectProgress.topics && Object.keys(subjectProgress.topics).length > 0) {
                    completedSubjects++;
                }
            });
            
            let totalTargets = userTargets.length + completedTargets.length;
            let achievedTargets = 0;
            let successfulTargets = 0;
            
            if (totalTargets > 0) {
                achievedTargets = completedTargets.length;
                
                completedTargets.forEach(target => {
                    if (target.actualHours && target.plannedHours) {
                        const achievement = (target.actualHours / target.plannedHours) * 100;
                        if (achievement >= 80) successfulTargets++;
                    }
                });
                
                userTargets.forEach(target => {
                    if (target.completed) {
                        achievedTargets++;
                        if (target.actualHours && target.plannedHours) {
                            const achievement = (target.actualHours / target.plannedHours) * 100;
                            if (achievement >= 80) successfulTargets++;
                        }
                    }
                });
                
                userStats.successRate = totalTargets > 0 ? Math.round((successfulTargets / totalTargets) * 100) : 0;
            }
            
            const today = new Date().toISOString().split('T')[0];
            const todayTargets = [...userTargets, ...completedTargets].filter(t => t.date === today);
            let dailyEfficiency = 0;
            
            if (todayTargets.length > 0) {
                const totalPlannedToday = todayTargets.reduce((sum, t) => sum + (t.plannedHours || 0), 0);
                const totalActualToday = todayTargets.reduce((sum, t) => sum + (t.actualHours || 0), 0);
                dailyEfficiency = totalPlannedToday > 0 ? 
                    Math.round((totalActualToday / totalPlannedToday) * 100) : 0;
            }
            
            const daysSinceStart = userStats.totalStudyDays || 1;
            userStats.dailyVelocity = daysSinceStart > 0 ? totalActualHours / daysSinceStart : 0;
            
            const last7Days = getLast7Days();
            let last7DaysHours = 0;
            last7Days.forEach(date => {
                const dayTargets = [...userTargets, ...completedTargets].filter(t => t.date === date);
                last7DaysHours += dayTargets.reduce((sum, t) => sum + (t.actualHours || 0), 0);
            });
            userStats.last7DaysVelocity = last7DaysHours / 7;
            
            const dailyTarget = userStudyPlan?.dailyHours || 4;
            const expectedHours = dailyTarget * daysSinceStart;
            const performanceRatio = totalActualHours / expectedHours;
            
            userStats.totalHoursStudied = totalActualHours;
            userStats.totalTargetsSet = totalTargets;
            userStats.totalTargetsAchieved = achievedTargets;
            userStats.totalSuccessfulTargets = successfulTargets;
            DataStorage.save('stats', userStats);
            
            const plannedHoursForFilter = currentSubjectFilter === 'all' ? 
                totalPlannedHours : 
                (userStudyPlan?.subjectHours?.[currentSubjectFilter] || 0);
            
            document.getElementById('actual-hours').textContent = Math.round(totalActualHours * 10) / 10;
            document.getElementById('planned-hours').textContent = plannedHoursForFilter;
            document.getElementById('overall-progress').textContent = `${Math.round((totalSubtopicsCompleted / totalSubtopics) * 100) || 0}%`;
            document.getElementById('subjects-completed').textContent = completedSubjects;
            document.getElementById('topics-completed').textContent = completedTopics;
            document.getElementById('total-topics').textContent = currentSubjectFilter === 'all' ? totalTopics : syllabusData[currentSubjectFilter]?.topics.length || 0;
            document.getElementById('plan-completion').textContent = `${Math.round((totalActualHours / plannedHoursForFilter) * 100) || 0}%`;
            document.getElementById('targets-achieved').textContent = `${achievedTargets}/${totalTargets}`;
            document.getElementById('success-rate').textContent = `${userStats.successRate}%`;
            document.getElementById('daily-efficiency').textContent = `${dailyEfficiency}%`;
            document.getElementById('current-pace').textContent = `${Math.round(userStats.dailyVelocity * 10) / 10} hrs/day`;
            
            const paceStatus = document.getElementById('pace-status');
            if (userStats.dailyVelocity >= dailyTarget * 1.1) {
                paceStatus.innerHTML = '<span class="performance-good">Ahead of Schedule</span>';
                paceStatus.className = 'kpi-subtext';
            } else if (userStats.dailyVelocity >= dailyTarget * 0.9) {
                paceStatus.innerHTML = '<span class="performance-good">On Track</span>';
                paceStatus.className = 'kpi-subtext';
            } else if (userStats.dailyVelocity >= dailyTarget * 0.5) {
                paceStatus.innerHTML = '<span class="performance-neutral">Slightly Behind</span>';
                paceStatus.className = 'kpi-subtext';
            } else {
                paceStatus.innerHTML = '<span class="performance-bad">Behind Schedule</span>';
                paceStatus.className = 'kpi-subtext';
            }
            
            const performanceIndicator = document.getElementById('performance-indicator');
            if (performanceRatio >= 1.1) {
                performanceIndicator.innerHTML = '<i class="fas fa-arrow-up performance-good"></i> <span>Overperforming</span>';
            } else if (performanceRatio >= 0.8) {
                performanceIndicator.innerHTML = '<i class="fas fa-check performance-good"></i> <span>On Track</span>';
            } else if (performanceRatio >= 0.5) {
                performanceIndicator.innerHTML = '<i class="fas fa-exclamation-triangle performance-neutral"></i> <span>Needs Improvement</span>';
            } else {
                performanceIndicator.innerHTML = '<i class="fas fa-arrow-down performance-bad"></i> <span>Underperforming</span>';
            }
            
            updateStreak();
        }
        
        function getLast7Days() {
            const dates = [];
            for (let i = 0; i < 7; i++) {
                const date = new Date();
                date.setDate(date.getDate() - i);
                dates.push(date.toISOString().split('T')[0]);
            }
            return dates;
        }
        
        function updateStreak() {
            const today = new Date().toISOString().split('T')[0];
            const lastStudy = userStats.lastStudyDate;
            
            const todayHours = [...userTargets, ...completedTargets]
                .filter(t => t.date === today)
                .reduce((sum, t) => sum + (t.actualHours || 0), 0);
            
            if (todayHours > 0) {
                if (!lastStudy) {
                    userStats.currentStreak = 1;
                } else {
                    const lastDate = new Date(lastStudy);
                    const todayDate = new Date(today);
                    const diffTime = Math.abs(todayDate - lastDate);
                    const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
                    
                    if (diffDays === 1) {
                        userStats.currentStreak++;
                    } else if (diffDays > 1) {
                        userStats.currentStreak = 1;
                    }
                }
                userStats.lastStudyDate = today;
            }
            
            userStats.bestStreak = Math.max(userStats.bestStreak, userStats.currentStreak);
            if (todayHours > 0) {
                userStats.totalStudyDays++;
            }
            
            DataStorage.save('stats', userStats);
            document.getElementById('streak-days').textContent = userStats.currentStreak;
        }
        
        // ====================
        // INITIALIZE APP
        // ====================
        function loadUserData() {
            const savedUser = DataStorage.load('user');
            if (savedUser) {
                currentUser = savedUser;
            } else {
                currentUser = {
                    name: "Satyam Kumar",
                    initials: "SK"
                };
                DataStorage.save('user', currentUser);
            }
            
            userProgress = DataStorage.load('progress') || initializeProgress();
            userTargets = DataStorage.load('targets') || [];
            completedTargets = DataStorage.load('completedTargets') || [];
            userStudyPlan = DataStorage.load('studyPlan') || initializeStudyPlan();
            userStats = DataStorage.load('stats') || initializeStats();
            userVideos = DataStorage.load('videos') || [];
            userResources = DataStorage.load('resources') || [];
            
            updateUserUI();
            updatePlanDisplay();
            updateDashboard();
            
            if (userVideos.length === 0 || userResources.length === 0) {
                loadDefaultContent();
            }
        }
        
        function initializeProgress() {
            const progress = { subjects: {} };
            
            for (const paperId in syllabusData) {
                progress.subjects[paperId] = {
                    completedTopics: 0,
                    totalTopics: syllabusData[paperId].topics.length,
                    topics: {}
                };
                
                syllabusData[paperId].topics.forEach(topic => {
                    progress.subjects[paperId].topics[topic.name] = {
                        status: 'not-started',
                        subtopics: {}
                    };
                    
                    topic.subtopics.forEach(subtopic => {
                        progress.subjects[paperId].topics[topic.name].subtopics[subtopic.name] = {
                            actualHours: 0,
                            completed: false
                        };
                    });
                });
            }
            
            return progress;
        }
        
        function initializeStudyPlan() {
            const today = new Date();
            const examDate = new Date();
            examDate.setDate(today.getDate() + 180);
            
            const plan = {
                daysToExam: 180,
                dailyHours: 4,
                totalHours: 720,
                startDate: today.toISOString().split('T')[0],
                examDate: examDate.toISOString().split('T')[0],
                subjectHours: {},
                customDistribution: false
            };
            
            for (const subjectId in syllabusData) {
                const subjectWeight = syllabusData[subjectId].weightage;
                plan.subjectHours[subjectId] = Math.round((subjectWeight / 100) * 720);
            }
            
            return plan;
        }
        
        function initializeStats() {
            return {
                totalStudyDays: 0,
                currentStreak: 0,
                bestStreak: 0,
                totalHoursStudied: 0,
                totalTargetsSet: 0,
                totalTargetsAchieved: 0,
                totalSuccessfulTargets: 0,
                successRate: 0,
                lastStudyDate: null,
                dailyVelocity: 0,
                last7DaysVelocity: 0
            };
        }
        
        function updateUserUI() {
            if (currentUser) {
                const initials = currentUser.name.split(' ').map(n => n[0]).join('').toUpperCase();
                userAvatar.textContent = initials.substring(0, 2);
                userName.textContent = currentUser.name;
            }
        }
        
        function loadDefaultContent() {
            if (userVideos.length === 0) {
                userVideos = [
                    {
                        id: Date.now(),
                        title: "Financial Reporting - Complete Guide",
                        subject: "paper1",
                        link: "https://youtube.com/playlist?list=example1",
                        author: "CA Expert",
                        addedDate: new Date().toISOString()
                    },
                    {
                        id: Date.now() + 1,
                        title: "Advanced Financial Management Tips",
                        subject: "paper2",
                        link: "https://youtube.com/playlist?list=example2",
                        author: "Finance Guru",
                        addedDate: new Date().toISOString()
                    }
                ];
                DataStorage.save('videos', userVideos);
            }
            
            if (userResources.length === 0) {
                userResources = [
                    {
                        id: Date.now(),
                        title: "FR Formula Sheet",
                        subject: "paper1",
                        type: "formulas",
                        link: "#",
                        author: "Satyam Kumar",
                        addedDate: new Date().toISOString()
                    },
                    {
                        id: Date.now() + 1,
                        title: "DT Important Sections",
                        subject: "paper4",
                        type: "notes",
                        link: "#",
                        author: "Satyam Kumar",
                        addedDate: new Date().toISOString()
                    }
                ];
                DataStorage.save('resources', userResources);
            }
        }
        
     async function activateLicense() {
    const licenseKey = licenseKeyInput.value.trim();
    
    if (!licenseKey) {
        showLicenseMessage('Please enter a license key', 'error');
        return;
    }
    
    // Show loading state
    const activateBtn = document.getElementById('activate-btn');
    const originalText = activateBtn.innerHTML;
    activateBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Validating...';
    activateBtn.disabled = true;
    
    try {
        const validation = EnhancedLicenseSystem.validateLicense(licenseKey);
        
        if (!validation.valid) {
            // Show specific error message
            let errorMessage = validation.error;
            let errorType = 'error';
            
            switch(validation.code) {
                case 'EXPIRED':
                    errorMessage = `License expired on ${new Date(validation.expiry).toLocaleDateString()}. Please renew.`;
                    errorType = 'warning';
                    break;
                case 'DEVICE_MISMATCH':
                    errorMessage = 'This license is registered to another device. Please contact support.';
                    errorType = 'warning';
                    break;
                case 'FORMAT_ERROR':
                    errorMessage = 'Invalid license format. Should start with CA-PRO-PREMIUM-';
                    break;
            }
            
            showLicenseMessage(errorMessage, errorType);
            
            // Show license status
            const statusIndicator = document.getElementById('license-status-indicator');
            const statusText = document.getElementById('license-status-text');
            
            statusIndicator.style.display = 'block';
            statusIndicator.className = `license-status-indicator ${errorType}`;
            statusText.innerHTML = `<i class="fas fa-exclamation-triangle"></i> ${errorMessage}`;
            
            return;
        }
        
        // Valid license - Save with enhanced data
        DataStorage.save('license', {
            key: licenseKey,
            activated: new Date().toISOString(),
            validation: validation,
            deviceId: EnhancedLicenseSystem.getDeviceId()
        });
        
        // Set current user
        currentUser = {
            name: validation.data.name,
            initials: validation.data.name.split(' ').map(n => n[0]).join('').toUpperCase(),
            licenseType: validation.type,
            expiry: validation.expiry,
            daysRemaining: validation.daysRemaining
        };
        
        DataStorage.save('user', currentUser);
        
        // Show success animation
        showLicenseMessage('License activated successfully! Redirecting...', 'success');
        
        // Add success animation
        const licenseBox = document.querySelector('.license-box');
        licenseBox.style.animation = 'none';
        setTimeout(() => {
            licenseBox.style.animation = 'pulse 0.5s ease';
        }, 10);
        
        // Transition to app
        setTimeout(() => {
            licenseContainer.style.display = 'none';
            appContainer.style.display = 'block';
            
            setupEventListeners();
            loadUserData();
            setupNotifications();
            
            // Show welcome toast with license info
            showToast(`Welcome ${currentUser.name}! Premium license active. ${validation.daysRemaining} days remaining.`, 'success');
            
            // Update user status in header
            updateLicenseStatusUI();
            
        }, 1500);
        
    } catch (error) {
        console.error('License activation error:', error);
        showLicenseMessage('An error occurred during activation. Please try again.', 'error');
    } finally {
        // Reset button
        activateBtn.innerHTML = originalText;
        activateBtn.disabled = false;
    }
}

function updateLicenseStatusUI() {
    const status = EnhancedLicenseSystem.getLicenseStatus();
    const userStatusElement = document.querySelector('.user-status');
    
    if (status.status === 'active') {
        if (status.daysRemaining <= 7) {
            userStatusElement.innerHTML = `<i class="fas fa-clock"></i> ${status.daysRemaining}d left`;
            userStatusElement.style.background = 'rgba(243, 156, 18, 0.1)' !important;
            userStatusElement.style.color = 'var(--warning)' !important;
            userStatusElement.style.borderColor = 'rgba(243, 156, 18, 0.2)' !important;
        } else {
            userStatusElement.innerHTML = `<i class="fas fa-crown"></i> Premium`;
            userStatusElement.style.background = 'rgba(52, 152, 219, 0.1)' !important;
            userStatusElement.style.color = 'var(--secondary)' !important;
            userStatusElement.style.borderColor = 'rgba(52, 152, 219, 0.2)' !important;
        }
        
        // Update user name with license info
        const userNameElement = document.getElementById('user-name');
        if (userNameElement) {
            userNameElement.innerHTML = `${currentUser.name} <small style="font-size: 12px; color: var(--gray);">(${status.daysRemaining}d)</small>`;
        }
    }
}
        
        // Additional functions (loadSubjectContent, saveStudyPlan, loadPlanningPage, etc.)
        // These are already implemented in your original code but truncated here for brevity
        // Make sure to include them from your original code
        
        // Note: The remaining functions like loadSubjectContent, saveStudyPlan, 
        // loadPlanningPage, addVideo, addResource, exportUserData, etc.
        // should be copied from your original code as they are already implemented.
        
    </script>
</body>
</html>

